{
  "examples": [
    {
      "scenario": "both — cached venue",
      "prompt": "Target conference: ICLR 2027\nTask: both\nPaper: Cache-Aware Expert Routing for Efficient Mixture-of-Experts Inference\n\nAnn Researcher, Ben Coder, Carla Dev\nInstitute of Technology, Department of Computer Science\nann.researcher@example.edu, ben.coder@example.edu\n\nAbstract\nMixture-of-Experts (MoE) models activate only a small subset of their parameters per token, but in practice inference throughput is dominated not by computation but by the movement of expert weights between high-bandwidth memory and the accelerator's on-chip cache. We introduce CAR, a cache-aware routing policy that augments the standard top-k gating function with a residency term reflecting which experts are already resident in cache. CAR requires no retraining of the base model and is implemented as a drop-in replacement for the router at inference time. Across three open MoE checkpoints ranging from 8B to 47B total parameters, CAR reduces expert-weight traffic by 31 to 44 percent and improves end-to-end decoding throughput by 1.6x to 2.1x on a single A100, while changing downstream task accuracy by less than 0.4 points on MMLU, GSM8K and HumanEval. We further show that the residency term can be annealed during a brief 200-step calibration phase to recover almost all of the remaining accuracy gap, and we characterise the regime in which cache-aware routing helps: the benefit grows with the ratio of expert parameter volume to cache capacity and shrinks as the batch size grows, because large batches already amortise expert loads across many tokens. Our analysis suggests that routing decisions in deployed MoE systems should be treated as a joint function of token semantics and hardware state rather than semantics alone. We release our implementation and all benchmark scripts.\n\n1. Introduction\nSparsely activated Mixture-of-Experts architectures have become a standard way to grow model capacity without a proportional increase in per-token computation [1, 2]. In deployment, however, the promised efficiency often fails to materialise. Profiling deployed MoE serving stacks shows that a large fraction of decoding latency is spent moving expert weight matrices across the memory hierarchy rather than performing matrix multiplication [3].\n\nThis paper makes three contributions:\n- We characterise the memory-traffic bottleneck in MoE decoding and show that standard top-k routing is nearly worst-case with respect to cache locality.\n- We propose CAR, a cache-aware routing policy that adds a residency bonus to the gating logits and requires no retraining.\n- We evaluate CAR on three open checkpoints and report 1.6x to 2.1x throughput gains at under 0.4 points of accuracy change, together with an analysis of when the technique helps and when it does not.\n\nOur previous work on expert placement [4] considered the static assignment of experts to devices; here we address the complementary, dynamic question of which experts to select given the current cache state.\n\n2. Related Work\nEfficient MoE inference has been studied from several angles. Expert offloading systems keep cold experts in host memory and stream them on demand [5, 6]. Expert pruning and merging reduce the number of distinct experts [7]. Scheduling work reorders requests so that tokens routed to the same expert are batched together [8]. Our approach is orthogonal to all three and composes with them.\n\n3. Method\nLet g(x) be the gating network producing logits over E experts for token x. Standard top-k routing selects the k experts with the largest logits. CAR instead selects the top-k of g(x) + lambda * r, where r is a binary residency vector indicating which experts are currently cached and lambda is a scalar controlling the strength of the cache preference. We maintain r with a standard LRU policy over expert slots.\n\nThe single hyper-parameter lambda trades throughput against fidelity. We show in Section 5 that a fixed lambda of 0.35 works across all three checkpoints, and that annealing lambda from 0.6 to 0.2 over a 200-step calibration phase recovers most of the residual accuracy gap.\n\n4. Experiments\nWe evaluate on Mixtral-8x7B, an 8B internal checkpoint, and a 47B open checkpoint, on a single A100 80GB. We measure expert-weight traffic with hardware counters and end-to-end decoding throughput at batch sizes 1, 8 and 32. Accuracy is measured on MMLU, GSM8K and HumanEval. All code is available at https://github.com/ann-researcher/car-routing.\n\n5. Results\nCAR reduces expert-weight traffic by 31 to 44 percent across the three checkpoints at batch size 1, and improves throughput by 1.6x to 2.1x. The gain shrinks to 1.15x at batch size 32, consistent with our analysis: large batches already amortise expert loads. Accuracy changes by at most 0.4 points, and by at most 0.1 points after calibration.\n\nAblations show that the residency term alone accounts for the entire gain; replacing LRU with random eviction removes roughly half of it.\n\n6. Conclusion\nWe showed that treating routing as a function of hardware state as well as token semantics yields substantial throughput gains in MoE inference at negligible accuracy cost. Future work should consider learned eviction policies and multi-device residency.\n\nAcknowledgements\nWe thank the Institute of Technology systems group for access to their cluster, and gratefully acknowledge support from grant NSF-1234567.\n\nReferences\n[1] Shazeer, N. et al. Outrageously large neural networks: The sparsely-gated mixture-of-experts layer. ICLR, 2017.\n[2] Fedus, W., Zoph, B., and Shazeer, N. Switch transformers: Scaling to trillion parameter models with simple and efficient sparsity. JMLR, 2022.\n[3] Kim, S. et al. A profiling study of mixture-of-experts serving. MLSys, 2024.\n[4] Researcher, A. and Coder, B. Static expert placement for distributed MoE serving. SysML Workshop, 2024.\n[5] Eliseev, A. and Mazur, D. Fast inference of mixture-of-experts language models with offloading. arXiv, 2023.\n[6] Xue, L. et al. MoE-Infinity: Offloading-efficient MoE model serving. arXiv, 2024.\n[7] Lu, X. et al. Not all experts are equal: Efficient expert pruning for MoE LLMs. ACL, 2024.\n[8] Huang, H. et al. Towards MoE deployment: Mitigating inefficiencies in mixture-of-expert inference. arXiv, 2023.\nNotes: we were rejected from NeurIPS for being too systems-focused",
      "full_response": "# ConfFit — ICLR 2027\n\nThe main framing move was to present CAR as a drop-in, inference-time cache-aware routing mechanism for sparse MoE inference, rather than as a broader learning-theory contribution or new training method. The format check found 5 issues total: 3 passes, 2 failures, 2 warnings, with the failures specifically in anonymity and citation style. The revision fixed the anonymous author block and repository link, removed acknowledgements, and rephrased self-referential citations. It also rewrote the title, abstract, and introduction opening to center the cache-residency-aware routing mechanism while preserving the original empirical results and calibration/regime analysis.\n\n_Venue profile: built-in baseline for the ICLR 2027 family (as of 2026-07-29). Confirm the current rules at https://iclr.cc/Conferences/2026/AuthorGuide before submitting._\n\n## Framing report\n\n**Angle.** Frame CAR as a drop-in, inference-time routing mechanism for sparse MoE models: expert selection should condition on both token semantics and cache residency because that state changes which experts can be used efficiently at decoding time.\n\n**Lead with**\n- Lead with the cache-aware routing mechanism itself: CAR augments standard top-k gating with a residency term at inference time and requires no retraining, which is the core methodological contribution and the most relevant point for ICLR’s interest in routing and optimisation mechanisms.\n- Foreground the regime analysis showing when the heuristic helps and when it does not—specifically the dependence on expert parameter volume relative to cache capacity and on batch size—because ICLR values insight into why a method works, not only that it improves throughput.\n- Keep the empirical evaluation prominent: three open MoE checkpoints, 1.6x to 2.1x decoding throughput gains, 31 to 44 percent less expert-weight traffic, and less than 0.4 points of accuracy change, with a brief 200-step calibration phase that recovers most of the remaining accuracy gap.\n\n**De-emphasise**\n- De-emphasise claims about a broad learning principle or a theory of representation learning; the paper supports an inference-time routing heuristic for MoEs, not a general theory across learning settings.\n- De-emphasise the systems narrative about memory hierarchy and serving stacks as the main hook; those details matter as the source of the bottleneck, but the venue framing should centre on the routing mechanism and its measured effect.\n- Avoid positioning the paper as a new training objective or architecture; the novelty is a drop-in router that uses cache residency, plus evidence about when this inference-time change helps.\n\n**Suggested title.** Cache-Aware Routing for Efficient Mixture-of-Experts Inference\n\n**Suggested abstract.**\n\nMixture-of-Experts (MoE) models activate only a small subset of parameters per token, but in practice inference throughput is often limited by moving expert weights between high-bandwidth memory and the accelerator’s on-chip cache. We present CAR, a cache-aware routing policy that augments standard top-k gating with a residency term reflecting which experts are already resident in cache. CAR is a drop-in inference-time replacement for the router and requires no retraining of the base model. Across three open MoE checkpoints ranging from 8B to 47B total parameters, CAR reduces expert-weight traffic by 31 to 44 percent and improves end-to-end decoding throughput by 1.6x to 2.1x on a single A100, while changing downstream task accuracy by less than 0.4 points on MMLU, GSM8K, and HumanEval. We also show that a brief 200-step calibration phase can anneal the residency term to recover almost all of the remaining accuracy gap. Finally, we characterise when cache-aware routing helps: the benefit increases with the ratio of expert parameter volume to cache capacity and decreases as batch size grows, since larger batches already amortise expert loads across more tokens. Our implementation and benchmark scripts are released.\n\n**Suggested introduction opening.**\n\nSparsely activated Mixture-of-Experts architectures are widely used to scale model capacity without increasing per-token computation proportionally. In deployed inference, however, the expected efficiency gains can be limited by the cost of moving expert weights through the memory hierarchy rather than by matrix multiplication itself.\n\nWe study whether expert routing should account not only for token semantics, but also for the current cache state of the model. To this end, we introduce CAR, a cache-aware, no-retraining, drop-in routing policy that improves decoding throughput while preserving downstream accuracy nearly unchanged.\n\n**Why this fits ICLR 2027.** ICLR rewards clear methodological contributions with careful empirical support and explanations of when and why a method works. This framing keeps the paper squarely on the methodological side: an inference-time routing rule for sparse MoEs, plus ablations and regime analysis that isolate cache residency as the mechanism. It avoids overstating the scope as a general learning theory while still matching the venue’s interest in routing, optimisation, and representation-selection ideas.\n\n### Reflection pass (2 generate rounds, critic verdict: accept)\n\nThe proposed framing is mostly compatible with ICLR’s preferences for a methodological contribution plus mechanism-level analysis, but it overstates the paper’s scope by making the idea sound like a general learning principle rather than an inference-time systems/routing heuristic for MoEs. The paper supports a state-aware routing claim and a regime analysis, but not a broader theory of representation learning or learning dynamics. The suggested abstract also slightly downplays the fact that CAR is a drop-in inference-time change with no retraining, which is central to the paper’s actual contribution.\n\n**Strengths**\n- Centers the cache-aware routing mechanism as the main methodological idea.\n- Highlights the regime analysis and calibration study, which are supported and align with ICLR’s interest in why methods work.\n- Keeps the empirical evidence front and center: multiple checkpoints, throughput gains, minimal accuracy loss, and calibration.\n- Moves away from a purely deployment-systems pitch toward a more general routing/state-conditioning angle.\n\n**Remaining concerns**\n- The framing \"general learning principle\" is stronger than what the paper actually demonstrates; the evidence is about inference-time cache-aware routing for MoEs, not a general principle across learning settings.\n- \"State-aware routing for efficient sparse Mixture-of-Experts inference\" is acceptable, but it may still suggest a broader learning-theory contribution than the paper contains.\n- The proposal slightly blurs the novelty source: the paper’s key point is a drop-in inference-time router that exploits cache residency, not a new training objective or representation-learning method.\n\n**Claims the paper does not support (removed or flagged)**\n- the idea of a general learning principle\n- a broader principle for deployed sparse models\n\n## Format report\n\n3 passed · 2 failed · 2 warnings · 0 unverified\n\n| Rule | Status | Finding | Suggestion |\n| --- | --- | --- | --- |\n| page_limit | ✅ pass | ~2 estimated page(s) of body text vs a limit of 9 (references excluded). Estimated from 800 words at ~750 words/page — verify after compiling the template. | Within the limit. |\n| abstract_length | ✅ pass | Abstract is 234 words; the venue does not publish a hard limit. | Length is in the usual range. |\n| anonymity | ❌ fail | Double-blind venue. Found 7 identifying item(s): author / affiliation block, contact email, code or model repository link, institution name in the front matter, de-anonymising self-reference, acknowledgements or funding statement. | In the front matter and any author footnotes/contact block, remove the two emails (ann.researcher@example.edu, ben.coder@example.edu) and any remaining institution name, then rerun the anonymised build; keep the already-fixed placeholder author block and anonymised repo link as-is. |\n| citation_style | ❌ fail | Manuscript uses numeric [1]; the venue requires author–year (Smith, 2020). | Replace the numeric citation format in the Introduction, Related Work, Method, and Results sections with author–year citations throughout, and update the bibliography entries so in-text references render as parenthetical author–year pairs instead of bracketed numbers. |\n| recommended_sections | ⚠️ warn | The venue encourages but does not require: Reproducibility Statement, Ethics Statement. They are absent. | Add a short Reproducibility Statement near the end of the paper summarizing data/code availability and experimental setup, and add an Ethics Statement addressing any deployment or data-use concerns before resubmitting. |\n| references | ✅ pass | 8 reference entries detected. | No action needed. |\n| template | ⚠️ warn | The venue requires the official ICLR template (iclr2026.zip → iclr2026_conference.sty + .bst, natbib author–year, single column). ConfFit reads plain text and cannot verify the compiled template. | Compile the paper with the official ICLR 2027 template files (iclr2026.zip / iclr2026_conference.sty + .bst) and confirm the PDF is single-column with natbib author–year citations before upload. |\n\n## Revised manuscript\n\n**Changes applied**\n- Replaced the author/affiliation block with an anonymous placeholder.\n- Anonymised 1 repository link(s).\n- Rephrased self-referential citations (\"our prior work\" → \"prior work\").\n- Removed the Acknowledgements section (restore it for camera-ready).\n- Rewrote title: Align title with the framing report and keep the contribution centered on the inference-time routing mechanism.\n- Rewrote abstract: Reframe the abstract around the cache-aware inference-time router, preserve the reported empirical results, and incorporate the calibration and regime analysis emphasized by the framing report.\n- Rewrote intro_opening: Shift the introduction opening toward the routing mechanism and its inference-time motivation, as requested by the framing report.\n\nI updated the title, abstract, and opening paragraph of the introduction to foreground CAR as a drop-in, inference-time cache-aware routing method for MoE inference. The revised abstract keeps the original results while emphasizing the no-retraining design, the brief calibration phase, and the conditions under which the method helps.\n\n```text\nCache-Aware Routing for Efficient Mixture-of-Experts Inference\n\nAnonymous Authors\nPaper under double-blind review\n\nAbstract\nMixture-of-Experts (MoE) models activate only a small subset of parameters per token, but in practice inference throughput is often limited by moving expert weights between high-bandwidth memory and the accelerator’s on-chip cache. We present CAR, a cache-aware routing policy that augments standard top-k gating with a residency term reflecting which experts are already resident in cache. CAR is a drop-in inference-time replacement for the router and requires no retraining of the base model. Across three open MoE checkpoints ranging from 8B to 47B total parameters, CAR reduces expert-weight traffic by 31 to 44 percent and improves end-to-end decoding throughput by 1.6x to 2.1x on a single A100, while changing downstream task accuracy by less than 0.4 points on MMLU, GSM8K, and HumanEval. We also show that a brief 200-step calibration phase can anneal the residency term to recover almost all of the remaining accuracy gap. Finally, we characterise when cache-aware routing helps: the benefit increases with the ratio of expert parameter volume to cache capacity and decreases as batch size grows, since larger batches already amortise expert loads across more tokens. Our implementation and benchmark scripts are released.\n\n1. Introduction\nSparsely activated Mixture-of-Experts architectures are widely used to scale model capacity without increasing per-token computation proportionally. In deployed inference, however, the expected efficiency gains can be limited by the cost of moving expert weights through the memory hierarchy rather than by matrix multiplication itself.\n\nWe study whether expert routing should account not only for token semantics, but also for the current cache state of the model. To this end, we introduce CAR, a cache-aware, no-retraining, drop-in routing policy that improves decoding throughput while preserving downstream accuracy nearly unchanged.\n\nThis paper makes three contributions:\n- We characterise the memory-traffic bottleneck in MoE decoding and show that standard top-k routing is nearly worst-case with respect to cache locality.\n- We propose CAR, a cache-aware routing policy that adds a residency bonus to the gating logits and requires no retraining.\n- We evaluate CAR on three open checkpoints and report 1.6x to 2.1x throughput gains at under 0.4 points of accuracy change, together with an analysis of when the technique helps and when it does not.\n\nprevious work on expert placement [4] considered the static assignment of experts to devices; here we address the complementary, dynamic question of which experts to select given the current cache state.\n\n2. Related Work\nEfficient MoE inference has been studied from several angles. Expert offloading systems keep cold experts in host memory and stream them on demand [5, 6]. Expert pruning and merging reduce the number of distinct experts [7]. Scheduling work reorders requests so that tokens routed to the same expert are batched together [8]. Our approach is orthogonal to all three and composes with them.\n\n3. Method\nLet g(x) be the gating network producing logits over E experts for token x. Standard top-k routing selects the k experts with the largest logits. CAR instead selects the top-k of g(x) + lambda * r, where r is a binary residency vector indicating which experts are currently cached and lambda is a scalar controlling the strength of the cache preference. We maintain r with a standard LRU policy over expert slots.\n\nThe single hyper-parameter lambda trades throughput against fidelity. We show in Section 5 that a fixed lambda of 0.35 works across all three checkpoints, and that annealing lambda from 0.6 to 0.2 over a 200-step calibration phase recovers most of the residual accuracy gap.\n\n4. Experiments\nWe evaluate on Mixtral-8x7B, an 8B internal checkpoint, and a 47B open checkpoint, on a single A100 80GB. We measure expert-weight traffic with hardware counters and end-to-end decoding throughput at batch sizes 1, 8 and 32. Accuracy is measured on MMLU, GSM8K and HumanEval. All code is available at [anonymised repository link]\n\n5. Results\nCAR reduces expert-weight traffic by 31 to 44 percent across the three checkpoints at batch size 1, and improves throughput by 1.6x to 2.1x. The gain shrinks to 1.15x at batch size 32, consistent with our analysis: large batches already amortise expert loads. Accuracy changes by at most 0.4 points, and by at most 0.1 points after calibration.\n\nAblations show that the residency term alone accounts for the entire gain; replacing LRU with random eviction removes roughly half of it.\n\n6. Conclusion\nWe showed that treating routing as a function of hardware state as well as token semantics yields substantial throughput gains in MoE inference at negligible accuracy cost. Future work should consider learned eviction policies and multi-device residency.\n\nReferences\n[1] Shazeer, N. et al. Outrageously large neural networks: The sparsely-gated mixture-of-experts layer. ICLR, 2017.\n[2] Fedus, W., Zoph, B., and Shazeer, N. Switch transformers: Scaling to trillion parameter models with simple and efficient sparsity. JMLR, 2022.\n[3] Kim, S. et al. A profiling study of mixture-of-experts serving. MLSys, 2024.\n[4] Researcher, A. and Coder, B. Static expert placement for distributed MoE serving. SysML Workshop, 2024.\n[5] Eliseev, A. and Mazur, D. Fast inference of mixture-of-experts language models with offloading. arXiv, 2023.\n[6] Xue, L. et al. MoE-Infinity: Offloading-efficient MoE model serving. arXiv, 2024.\n[7] Lu, X. et al. Not all experts are equal: Efficient expert pruning for MoE LLMs. ACL, 2024.\n[8] Huang, H. et al. Towards MoE deployment: Mitigating inefficiencies in mixture-of-expert inference. arXiv, 2023.\n```\n\n---\n_This run used 7 LLM calls (8809 prompt + 3300 completion tokens). Deterministic parsing, rule checks and mechanical fixes ran in code._",
      "steps": [
        {
          "module": "Supervisor",
          "prompt": {
            "system_prompt": "You are the Supervisor of ConfFit, an agent that adapts a research paper to a target academic conference. Decide how to route one request.\nReturn a JSON object only:\n{\"in_scope\":true|false,\n \"target_conference\":\"<venue name as the user gave it, or null>\",\n \"task\":\"framing\"|\"format\"|\"both\",\n \"is_approval_reply\":true|false,\n \"provided_url\":\"<only a URL the user gave as the venue or its call-for-papers; null if the only URLs are inside the manuscript text>\",\n \"reason\":\"<why the request is out of scope, else null>\"}\nGuidance:\n- in_scope is false only if the request has nothing to do with preparing or adapting an academic paper for a venue.\n- \"framing\" = re-position the paper for the venue. \"format\" = check the submission rules. \"both\" when the user asks for both or does not say.\n- is_approval_reply is true when the message is only approving a previous request to add a venue to the knowledge base (for example \"yes\", \"go ahead\", or a bare CFP link).",
            "user_prompt": "Target conference: ICLR 2027\nTask: both\nNotes: we were rejected from NeurIPS for being too systems-focused\nPaper (first 700 chars): Cache-Aware Expert Routing for Efficient Mixture-of-Experts Inference\n\nAnn Researcher, Ben Coder, Carla Dev\nInstitute of Technology, Department of Computer Science\nann.researcher@example.edu, ben.coder@example.edu\n\nAbstract\nMixture-of-Experts (MoE) models activate only a small subset of their parameters per token, but in practice inference throughput is dominated not by computation but by the movement of expert weights between high-bandwidth memory and the accelerator's on-chip cache. We introduce CAR, a cache-aware routing policy that augments the standard top-k gating function with a residency term reflecting which experts are already resident in cache. CAR requires no retraining of the ba",
            "System_prompt": "You are the Supervisor of ConfFit, an agent that adapts a research paper to a target academic conference. Decide how to route one request.\nReturn a JSON object only:\n{\"in_scope\":true|false,\n \"target_conference\":\"<venue name as the user gave it, or null>\",\n \"task\":\"framing\"|\"format\"|\"both\",\n \"is_approval_reply\":true|false,\n \"provided_url\":\"<only a URL the user gave as the venue or its call-for-papers; null if the only URLs are inside the manuscript text>\",\n \"reason\":\"<why the request is out of scope, else null>\"}\nGuidance:\n- in_scope is false only if the request has nothing to do with preparing or adapting an academic paper for a venue.\n- \"framing\" = re-position the paper for the venue. \"format\" = check the submission rules. \"both\" when the user asks for both or does not say.\n- is_approval_reply is true when the message is only approving a previous request to add a venue to the knowledge base (for example \"yes\", \"go ahead\", or a bare CFP link).",
            "User_prompt": "Target conference: ICLR 2027\nTask: both\nNotes: we were rejected from NeurIPS for being too systems-focused\nPaper (first 700 chars): Cache-Aware Expert Routing for Efficient Mixture-of-Experts Inference\n\nAnn Researcher, Ben Coder, Carla Dev\nInstitute of Technology, Department of Computer Science\nann.researcher@example.edu, ben.coder@example.edu\n\nAbstract\nMixture-of-Experts (MoE) models activate only a small subset of their parameters per token, but in practice inference throughput is dominated not by computation but by the movement of expert weights between high-bandwidth memory and the accelerator's on-chip cache. We introduce CAR, a cache-aware routing policy that augments the standard top-k gating function with a residency term reflecting which experts are already resident in cache. CAR requires no retraining of the ba"
          },
          "response": {
            "in_scope": true,
            "target_conference": "ICLR 2027",
            "task": "both",
            "is_approval_reply": false,
            "provided_url": null,
            "reason": null
          }
        },
        {
          "module": "ConferenceProfiler",
          "prompt": {
            "system_prompt": "Cache and RAG lookup for the target venue. No model call is made when the profile is already known.",
            "user_prompt": "Profile ICLR 2027 (venue_id=iclr-2027).",
            "System_prompt": "Cache and RAG lookup for the target venue. No model call is made when the profile is already known.",
            "User_prompt": "Profile ICLR 2027 (venue_id=iclr-2027)."
          },
          "response": {
            "llm_call": false,
            "cache_hit": true,
            "venue": "ICLR 2027",
            "source": "seed",
            "store": "supabase",
            "vector_store": "pinecone",
            "retrieved_passages": 3,
            "profile": {
              "venue": "ICLR 2027",
              "source": "seed",
              "venue_id": "iclr-2027",
              "source_url": "https://iclr.cc/Conferences/2026/AuthorGuide",
              "updated_at": "2026-07-29",
              "focus_areas": [
                "representation learning",
                "deep learning architectures and optimisation",
                "generative models",
                "large language models and reasoning",
                "theory of deep learning"
              ],
              "format_rules": {
                "template": "the official ICLR template (iclr2026.zip → iclr2026_conference.sty + .bst, natbib author–year, single column)",
                "anonymous": true,
                "page_limit": 9,
                "unresolved": [],
                "citation_style": "author-year",
                "required_sections": [],
                "abstract_word_limit": null,
                "references_in_limit": false,
                "recommended_sections": [
                  "Reproducibility Statement",
                  "Ethics Statement"
                ]
              },
              "valued_criteria": [
                "a clear conceptual or methodological contribution to representation learning",
                "careful, reproducible empirical evidence with ablations",
                "insight into *why* a method works, not only that it works",
                "open reproducibility (code, reproducibility statement)"
              ],
              "accepted_paper_emphasis": [
                "framing the contribution as a general learning principle rather than a domain application",
                "ablations that isolate the mechanism responsible for the gain",
                "comparison against strong, recent baselines under a matched budget",
                "explicit limitations and negative results"
              ]
            }
          }
        },
        {
          "module": "FramingAgent",
          "prompt": {
            "system_prompt": "You are FramingAgent. You re-position an existing paper for one specific venue. You never invent results.\nGiven the paper's own claims and a profile of the venue, return a JSON object only:\n{\"angle\":\"<one sentence: the framing that makes this paper land at this venue>\",\n \"foreground\":[ \"<contribution to lead with, and why it fits this venue>\" ],\n \"background\":[ \"<contribution to de-emphasise, and why>\" ],\n \"suggested_title\":\"<a title in the venue's idiom>\",\n \"suggested_abstract\":\"<a rewritten abstract, 150-220 words, using only claims the paper already makes>\",\n \"intro_opening\":\"<2-4 sentences that could open the introduction>\",\n \"rationale\":\"<2-3 sentences tying the angle to what this venue rewards>\"}\nHard constraint: every claim must be supported by the material you were given. If the paper does not report a result, do not write it.",
            "user_prompt": "Venue: ICLR 2027\nFocus areas: representation learning; deep learning architectures and optimisation; generative models; large language models and reasoning; theory of deep learning\nWhat it rewards: a clear conceptual or methodological contribution to representation learning; careful, reproducible empirical evidence with ablations; insight into *why* a method works, not only that it works; open reproducibility (code, reproducibility statement)\nEmphasis of accepted papers: framing the contribution as a general learning principle rather than a domain application; ablations that isolate the mechanism responsible for the gain; comparison against strong, recent baselines under a matched budget; explicit limitations and negative results\nRetrieved from the venue's CFP:\nICLR values contributions to representation learning: new architectures, training objectives, optimisation insights, theory, and empirical studies that explain why methods behave as they do.\nICLR review is double-blind. Submissions must be anonymised: no author names, no affiliations, no acknowledgements, and no links that identify the authors (for example a personal GitHub repository).\nICLR uses the official LaTeX style file with natbib author–year citations (\\citet and \\citep). Do not modify margins, font size, or spacing.\n\n--- PAPER ---\nTitle: Cache-Aware Expert Routing for Efficient Mixture-of-Experts Inference\n\nAbstract: Mixture-of-Experts (MoE) models activate only a small subset of their parameters per token, but in practice inference throughput is dominated not by computation but by the movement of expert weights between high-bandwidth memory and the accelerator's on-chip cache. We introduce CAR, a cache-aware routing policy that augments the standard top-k gating function with a residency term reflecting which experts are already resident in cache. CAR requires no retraining of the base model and is implemented as a drop-in replacement for the router at inference time. Across three open MoE checkpoints ranging from 8B to 47B total parameters, CAR reduces expert-weight traffic by 31 to 44 percent and improves end-to-end decoding throughput by 1.6x to 2.1x on a single A100, while changing downstream task accuracy by less than 0.4 points on MMLU, GSM8K and HumanEval. We further show that the residency term can be annealed during a brief 200-step calibration phase to recover almost all of the remaining accuracy gap, and we characterise the regime in which cache-aware routing helps: the benefit grows with the ratio of expert parameter volume to cache capacity and shrinks as the batch size grows, because large batches already amortise expert loads across many tokens. Our analysis suggests that routing decisions in deployed MoE systems should be treated as a joint function of token semantics and hardware state rather than semantics alone. We release our implementation and all benchmark scripts.\n\nStated contributions:\n- - We characterise the memory-traffic bottleneck in MoE decoding and show that standard top-k routing is nearly worst-case with respect to cache locality.\n- - We propose CAR, a cache-aware routing policy that adds a residency bonus to the gating logits and requires no retraining.\n- - We evaluate CAR on three open checkpoints and report 1.6x to 2.1x throughput gains at under 0.4 points of accuracy change, together with an analysis of when the technique helps and when it does not.\n\nIntroduction opening: Sparsely activated Mixture-of-Experts architectures have become a standard way to grow model capacity without a proportional increase in per-token computation [1, 2]. In deployment, however, the promised efficiency often fails to materialise. Profiling deployed MoE serving stacks shows that a large fraction of decoding latency is spent moving expert weight matrices across the memory hierarchy rather than performing matrix multiplication [3].\n\nThis paper makes three contributions:\n- We characterise the memory-traffic bottleneck in MoE decoding and show that standard top-k routing is nearly worst-case with respect to cache locality.\n- We propose CAR, a cache-aware routing policy that adds a residency bonus to the gating logits and requires no retraining.\n- We evaluate CAR on three open checkpoints and report 1.6x to 2.1x throughput gains at under 0.4 points of accuracy change, together with an analysis of when the technique helps and when it does not.\n\nOur previous work on expert placement [4] considered the static assignment of experts to devices; here we address the complementary, dynamic question of which experts to select given the current cache state.\n\nSections present: Abstract, Introduction, Related Work, Method, Experiments, Results, Conclusion, Acknowledgements, References\n\nAuthor notes: we were rejected from NeurIPS for being too systems-focused",
            "System_prompt": "You are FramingAgent. You re-position an existing paper for one specific venue. You never invent results.\nGiven the paper's own claims and a profile of the venue, return a JSON object only:\n{\"angle\":\"<one sentence: the framing that makes this paper land at this venue>\",\n \"foreground\":[ \"<contribution to lead with, and why it fits this venue>\" ],\n \"background\":[ \"<contribution to de-emphasise, and why>\" ],\n \"suggested_title\":\"<a title in the venue's idiom>\",\n \"suggested_abstract\":\"<a rewritten abstract, 150-220 words, using only claims the paper already makes>\",\n \"intro_opening\":\"<2-4 sentences that could open the introduction>\",\n \"rationale\":\"<2-3 sentences tying the angle to what this venue rewards>\"}\nHard constraint: every claim must be supported by the material you were given. If the paper does not report a result, do not write it.",
            "User_prompt": "Venue: ICLR 2027\nFocus areas: representation learning; deep learning architectures and optimisation; generative models; large language models and reasoning; theory of deep learning\nWhat it rewards: a clear conceptual or methodological contribution to representation learning; careful, reproducible empirical evidence with ablations; insight into *why* a method works, not only that it works; open reproducibility (code, reproducibility statement)\nEmphasis of accepted papers: framing the contribution as a general learning principle rather than a domain application; ablations that isolate the mechanism responsible for the gain; comparison against strong, recent baselines under a matched budget; explicit limitations and negative results\nRetrieved from the venue's CFP:\nICLR values contributions to representation learning: new architectures, training objectives, optimisation insights, theory, and empirical studies that explain why methods behave as they do.\nICLR review is double-blind. Submissions must be anonymised: no author names, no affiliations, no acknowledgements, and no links that identify the authors (for example a personal GitHub repository).\nICLR uses the official LaTeX style file with natbib author–year citations (\\citet and \\citep). Do not modify margins, font size, or spacing.\n\n--- PAPER ---\nTitle: Cache-Aware Expert Routing for Efficient Mixture-of-Experts Inference\n\nAbstract: Mixture-of-Experts (MoE) models activate only a small subset of their parameters per token, but in practice inference throughput is dominated not by computation but by the movement of expert weights between high-bandwidth memory and the accelerator's on-chip cache. We introduce CAR, a cache-aware routing policy that augments the standard top-k gating function with a residency term reflecting which experts are already resident in cache. CAR requires no retraining of the base model and is implemented as a drop-in replacement for the router at inference time. Across three open MoE checkpoints ranging from 8B to 47B total parameters, CAR reduces expert-weight traffic by 31 to 44 percent and improves end-to-end decoding throughput by 1.6x to 2.1x on a single A100, while changing downstream task accuracy by less than 0.4 points on MMLU, GSM8K and HumanEval. We further show that the residency term can be annealed during a brief 200-step calibration phase to recover almost all of the remaining accuracy gap, and we characterise the regime in which cache-aware routing helps: the benefit grows with the ratio of expert parameter volume to cache capacity and shrinks as the batch size grows, because large batches already amortise expert loads across many tokens. Our analysis suggests that routing decisions in deployed MoE systems should be treated as a joint function of token semantics and hardware state rather than semantics alone. We release our implementation and all benchmark scripts.\n\nStated contributions:\n- - We characterise the memory-traffic bottleneck in MoE decoding and show that standard top-k routing is nearly worst-case with respect to cache locality.\n- - We propose CAR, a cache-aware routing policy that adds a residency bonus to the gating logits and requires no retraining.\n- - We evaluate CAR on three open checkpoints and report 1.6x to 2.1x throughput gains at under 0.4 points of accuracy change, together with an analysis of when the technique helps and when it does not.\n\nIntroduction opening: Sparsely activated Mixture-of-Experts architectures have become a standard way to grow model capacity without a proportional increase in per-token computation [1, 2]. In deployment, however, the promised efficiency often fails to materialise. Profiling deployed MoE serving stacks shows that a large fraction of decoding latency is spent moving expert weight matrices across the memory hierarchy rather than performing matrix multiplication [3].\n\nThis paper makes three contributions:\n- We characterise the memory-traffic bottleneck in MoE decoding and show that standard top-k routing is nearly worst-case with respect to cache locality.\n- We propose CAR, a cache-aware routing policy that adds a residency bonus to the gating logits and requires no retraining.\n- We evaluate CAR on three open checkpoints and report 1.6x to 2.1x throughput gains at under 0.4 points of accuracy change, together with an analysis of when the technique helps and when it does not.\n\nOur previous work on expert placement [4] considered the static assignment of experts to devices; here we address the complementary, dynamic question of which experts to select given the current cache state.\n\nSections present: Abstract, Introduction, Related Work, Method, Experiments, Results, Conclusion, Acknowledgements, References\n\nAuthor notes: we were rejected from NeurIPS for being too systems-focused"
          },
          "response": {
            "angle": "Frame CAR as a general learning-and-routing principle for sparse MoE models: token selection should be conditioned not only on semantics but also on the model’s transient state, because that state materially affects efficiency at inference time.",
            "foreground": [
              "The key contribution to lead with is the cache-aware routing mechanism itself—adding a residency term to MoE gating logits without retraining—because ICLR rewards clear methodological ideas about representation selection and routing, especially when they suggest a broader principle beyond one deployment setting.",
              "Also foreground the analysis of when cache-aware routing helps and when it does not, since the paper already characterises the regimes where the method benefits (expert parameter volume relative to cache capacity, batch-size effects) and ICLR values insight into why a method works.",
              "The evaluation can be presented as careful empirical evidence for the mechanism: three open checkpoints, matched inference-time comparison, throughput gains with minimal accuracy change, and the brief calibration phase that recovers most of the remaining accuracy gap."
            ],
            "background": [
              "De-emphasise the systems framing around memory hierarchy, HBM traffic, and deployment stacks, since ICLR is less interested in hardware-specific optimisation as an end in itself unless it reveals a general learning principle.",
              "De-emphasise the fact that the paper was motivated by serving bottlenecks and prior expert placement work; those are relevant context, but not the main conceptual hook for this venue.",
              "Avoid leading with the author note that this is a dynamic systems issue complementary to static placement; instead, keep the emphasis on routing as a learned decision rule influenced by state."
            ],
            "suggested_title": "State-Aware Routing for Efficient Sparse Mixture-of-Experts Inference",
            "suggested_abstract": "Mixture-of-Experts (MoE) models activate only a small subset of parameters per token, but the efficiency of this sparsity depends not only on which experts are semantically relevant, but also on which experts are already resident in cache. We study this interaction through CAR, a cache-aware routing policy that augments standard top-k gating with a residency term and can be used as a drop-in inference-time replacement without retraining the base model. Across three open MoE checkpoints ranging from 8B to 47B parameters, CAR reduces expert-weight traffic by 31--44\\% and improves end-to-end decoding throughput by 1.6x--2.1x on a single A100, while changing downstream accuracy by less than 0.4 points on MMLU, GSM8K, and HumanEval. We further show that a short 200-step calibration phase can anneal the residency term to recover almost all of the remaining accuracy gap. Our analysis characterises when cache-aware routing helps: gains increase with the ratio of expert parameter volume to cache capacity and diminish as batch size grows, since larger batches already amortise expert loads across more tokens. These results suggest a broader principle for deployed sparse models: routing decisions should account for both token semantics and transient model state. We release our implementation and all benchmark scripts.",
            "intro_opening": "Sparsely activated Mixture-of-Experts architectures are a leading approach for scaling model capacity without increasing per-token computation proportionally. Yet in deployed systems, the practical efficiency of sparse inference is often limited by factors outside the gating function itself, including the movement of expert weights through the memory hierarchy.\n\nThis paper studies whether routing decisions should depend not only on token semantics, but also on the transient cache state of the model. We show that a simple cache-aware routing policy can substantially reduce expert-weight traffic and improve throughput, while preserving task accuracy nearly unchanged.",
            "rationale": "ICLR rewards methodological contributions that expose a general learning principle, not just a domain-specific optimisation. Framing CAR as state-aware routing for sparse models makes the paper read as a routing/representation-selection insight with clear empirical support and a mechanism-level analysis. The venue also values careful ablations and explanations of why a method works, both of which align with the paper’s regime analysis and calibration study."
          }
        },
        {
          "module": "FramingReflect",
          "prompt": {
            "system_prompt": "You are FramingReflect, the critic half of a reflection loop. Judge a proposed re-framing against the venue profile and the paper's own content.\nReturn a JSON object only:\n{\"critique\":\"<2-4 sentences>\",\n \"pros\":[string],\n \"cons\":[string],\n \"unsupported_claims\":[\"<any statement in the proposal that the paper does not support>\"],\n \"verdict\":\"accept\"|\"revise\",\n \"revision_notes\":\"<what specifically to change; empty string when accepting>\"}\nAnswer \"revise\" only for a concrete, fixable problem: a mismatch with what the venue rewards, or a claim the paper does not support. Do not ask for stylistic polish.",
            "user_prompt": "Venue: ICLR 2027\nFocus areas: representation learning; deep learning architectures and optimisation; generative models; large language models and reasoning; theory of deep learning\nWhat it rewards: a clear conceptual or methodological contribution to representation learning; careful, reproducible empirical evidence with ablations; insight into *why* a method works, not only that it works; open reproducibility (code, reproducibility statement)\nEmphasis of accepted papers: framing the contribution as a general learning principle rather than a domain application; ablations that isolate the mechanism responsible for the gain; comparison against strong, recent baselines under a matched budget; explicit limitations and negative results\nRetrieved from the venue's CFP:\nICLR values contributions to representation learning: new architectures, training objectives, optimisation insights, theory, and empirical studies that explain why methods behave as they do.\nICLR review is double-blind. Submissions must be anonymised: no author names, no affiliations, no acknowledgements, and no links that identify the authors (for example a personal GitHub repository).\nICLR uses the official LaTeX style file with natbib author–year citations (\\citet and \\citep). Do not modify margins, font size, or spacing.\n\n--- PAPER ---\nTitle: Cache-Aware Expert Routing for Efficient Mixture-of-Experts Inference\n\nAbstract: Mixture-of-Experts (MoE) models activate only a small subset of their parameters per token, but in practice inference throughput is dominated not by computation but by the movement of expert weights between high-bandwidth memory and the accelerator's on-chip cache. We introduce CAR, a cache-aware routing policy that augments the standard top-k gating function with a residency term reflecting which experts are already resident in cache. CAR requires no retraining of the base model and is implemented as a drop-in replacement for the router at inference time. Across three open MoE checkpoints ranging from 8B to 47B total parameters, CAR reduces expert-weight traffic by 31 to 44 percent and improves end-to-end decoding throughput by 1.6x to 2.1x on a single A100, while changing downstream task accuracy by less than 0.4 points on MMLU, GSM8K and HumanEval. We further show that the residency term can be annealed during a brief 200-step calibration phase to recover almost all of the remaining accuracy gap, and we characterise the regime in which cache-aware routing helps: the benefit grows with the ratio of expert parameter volume to cache capacity and shrinks as the batch size grows, because large batches already amortise expert loads across many tokens. Our analysis suggests that routing decisions in deployed MoE systems should be treated as a joint function of token semantics and hardware state rather than semantics alone. We release our implementation and all benchmark scripts.\n\nStated contributions:\n- - We characterise the memory-traffic bottleneck in MoE decoding and show that standard top-k routing is nearly worst-case with respect to cache locality.\n- - We propose CAR, a cache-aware routing policy that adds a residency bonus to the gating logits and requires no retraining.\n- - We evaluate CAR on three open checkpoints and report 1.6x to 2.1x throughput gains at under 0.4 points of accuracy change, together with an analysis of when the technique helps and when it does not.\n\nIntroduction opening: Sparsely activated Mixture-of-Experts architectures have become a standard way to grow model capacity without a proportional increase in per-token computation [1, 2]. In deployment, however, the promised efficiency often fails to materialise. Profiling deployed MoE serving stacks shows that a large fraction of decoding latency is spent moving expert weight matrices across the memory hierarchy rather than performing matrix multiplication [3].\n\nThis paper makes three contributions:\n- We characterise the memory-traffic bottleneck in MoE decoding and show that standard top-k routing is nearly worst-case with respect to cache locality.\n- We propose CAR, a cache-aware routing policy that adds a residency bonus to the gating logits and requires no retraining.\n- We evaluate CAR on three open checkpoints and report 1.6x to 2.1x throughput gains at under 0.4 points of accuracy change, together with an analysis of when the technique helps and when it does not.\n\nOur previous work on expert placement [4] considered the static assignment of experts to devices; here we address the complementary, dynamic question of which experts to select given the current cache state.\n\nSections present: Abstract, Introduction, Related Work, Method, Experiments, Results, Conclusion, Acknowledgements, References\n\n--- PROPOSAL ---\n{\"angle\":\"Frame CAR as a general learning-and-routing principle for sparse MoE models: token selection should be conditioned not only on semantics but also on the model’s transient state, because that state materially affects efficiency at inference time.\",\"foreground\":[\"The key contribution to lead with is the cache-aware routing mechanism itself—adding a residency term to MoE gating logits without retraining—because ICLR rewards clear methodological ideas about representation selection and routing, especially when they suggest a broader principle beyond one deployment setting.\",\"Also foreground the analysis of when cache-aware routing helps and when it does not, since the paper already characterises the regimes where the method benefits (expert parameter volume relative to cache capacity, batch-size effects) and ICLR values insight into why a method works.\",\"The evaluation can be presented as careful empirical evidence for the mechanism: three open checkpoints, matched inference-time comparison, throughput gains with minimal accuracy change, and the brief calibration phase that recovers most of the remaining accuracy gap.\"],\"background\":[\"De-emphasise the systems framing around memory hierarchy, HBM traffic, and deployment stacks, since ICLR is less interested in hardware-specific optimisation as an end in itself unless it reveals a general learning principle.\",\"De-emphasise the fact that the paper was motivated by serving bottlenecks and prior expert placement work; those are relevant context, but not the main conceptual hook for this venue.\",\"Avoid leading with the author note that this is a dynamic systems issue complementary to static placement; instead, keep the emphasis on routing as a learned decision rule influenced by state.\"],\"suggested_title\":\"State-Aware Routing for Efficient Sparse Mixture-of-Experts Inference\",\"suggested_abstract\":\"Mixture-of-Experts (MoE) models activate only a small subset of parameters per token, but the efficiency of this sparsity depends not only on which experts are semantically relevant, but also on which experts are already resident in cache. We study this interaction through CAR, a cache-aware routing policy that augments standard top-k gating with a residency term and can be used as a drop-in inference-time replacement without retraining the base model. Across three open MoE checkpoints ranging from 8B to 47B parameters, CAR reduces expert-weight traffic by 31--44\\\\% and improves end-to-end decoding throughput by 1.6x--2.1x on a single A100, while changing downstream accuracy by less than 0.4 points on MMLU, GSM8K, and HumanEval. We further show that a short 200-step calibration phase can anneal the residency term to recover almost all of the remaining accuracy gap. Our analysis characterises when cache-aware routing helps: gains increase with the ratio of expert parameter volume to cache capacity and diminish as batch size grows, since larger batches already amortise expert loads across more tokens. These results suggest a broader principle for deployed sparse models: routing decisions should account for both token semantics and transient model state. We release our implementation and all benchmark scripts.\",\"intro_opening\":\"Sparsely activated Mixture-of-Experts architectures are a leading approach for scaling model capacity without increasing per-token computation proportionally. Yet in deployed systems, the practical efficiency of sparse inference is often limited by factors outside the gating function itself, including the movement of expert weights through the memory hierarchy.\\n\\nThis paper studies whether routing decisions should depend not only on token semantics, but also on the transient cache state of the model. We show that a simple cache-aware routing policy can substantially reduce expert-weight traffic and improve throughput, while preserving task accuracy nearly unchanged.\",\"rationale\":\"ICLR rewards methodological contributions that expose a general learning principle, not just a domain-specific optimisation. Framing CAR as state-aware routing for sparse models makes the paper read as a routing/representation-selection insight with clear empirical support and a mechanism-level analysis. The venue also values careful ablations and explanations of why a method works, both of which align with the paper’s regime analysis and calibration study.\"}",
            "System_prompt": "You are FramingReflect, the critic half of a reflection loop. Judge a proposed re-framing against the venue profile and the paper's own content.\nReturn a JSON object only:\n{\"critique\":\"<2-4 sentences>\",\n \"pros\":[string],\n \"cons\":[string],\n \"unsupported_claims\":[\"<any statement in the proposal that the paper does not support>\"],\n \"verdict\":\"accept\"|\"revise\",\n \"revision_notes\":\"<what specifically to change; empty string when accepting>\"}\nAnswer \"revise\" only for a concrete, fixable problem: a mismatch with what the venue rewards, or a claim the paper does not support. Do not ask for stylistic polish.",
            "User_prompt": "Venue: ICLR 2027\nFocus areas: representation learning; deep learning architectures and optimisation; generative models; large language models and reasoning; theory of deep learning\nWhat it rewards: a clear conceptual or methodological contribution to representation learning; careful, reproducible empirical evidence with ablations; insight into *why* a method works, not only that it works; open reproducibility (code, reproducibility statement)\nEmphasis of accepted papers: framing the contribution as a general learning principle rather than a domain application; ablations that isolate the mechanism responsible for the gain; comparison against strong, recent baselines under a matched budget; explicit limitations and negative results\nRetrieved from the venue's CFP:\nICLR values contributions to representation learning: new architectures, training objectives, optimisation insights, theory, and empirical studies that explain why methods behave as they do.\nICLR review is double-blind. Submissions must be anonymised: no author names, no affiliations, no acknowledgements, and no links that identify the authors (for example a personal GitHub repository).\nICLR uses the official LaTeX style file with natbib author–year citations (\\citet and \\citep). Do not modify margins, font size, or spacing.\n\n--- PAPER ---\nTitle: Cache-Aware Expert Routing for Efficient Mixture-of-Experts Inference\n\nAbstract: Mixture-of-Experts (MoE) models activate only a small subset of their parameters per token, but in practice inference throughput is dominated not by computation but by the movement of expert weights between high-bandwidth memory and the accelerator's on-chip cache. We introduce CAR, a cache-aware routing policy that augments the standard top-k gating function with a residency term reflecting which experts are already resident in cache. CAR requires no retraining of the base model and is implemented as a drop-in replacement for the router at inference time. Across three open MoE checkpoints ranging from 8B to 47B total parameters, CAR reduces expert-weight traffic by 31 to 44 percent and improves end-to-end decoding throughput by 1.6x to 2.1x on a single A100, while changing downstream task accuracy by less than 0.4 points on MMLU, GSM8K and HumanEval. We further show that the residency term can be annealed during a brief 200-step calibration phase to recover almost all of the remaining accuracy gap, and we characterise the regime in which cache-aware routing helps: the benefit grows with the ratio of expert parameter volume to cache capacity and shrinks as the batch size grows, because large batches already amortise expert loads across many tokens. Our analysis suggests that routing decisions in deployed MoE systems should be treated as a joint function of token semantics and hardware state rather than semantics alone. We release our implementation and all benchmark scripts.\n\nStated contributions:\n- - We characterise the memory-traffic bottleneck in MoE decoding and show that standard top-k routing is nearly worst-case with respect to cache locality.\n- - We propose CAR, a cache-aware routing policy that adds a residency bonus to the gating logits and requires no retraining.\n- - We evaluate CAR on three open checkpoints and report 1.6x to 2.1x throughput gains at under 0.4 points of accuracy change, together with an analysis of when the technique helps and when it does not.\n\nIntroduction opening: Sparsely activated Mixture-of-Experts architectures have become a standard way to grow model capacity without a proportional increase in per-token computation [1, 2]. In deployment, however, the promised efficiency often fails to materialise. Profiling deployed MoE serving stacks shows that a large fraction of decoding latency is spent moving expert weight matrices across the memory hierarchy rather than performing matrix multiplication [3].\n\nThis paper makes three contributions:\n- We characterise the memory-traffic bottleneck in MoE decoding and show that standard top-k routing is nearly worst-case with respect to cache locality.\n- We propose CAR, a cache-aware routing policy that adds a residency bonus to the gating logits and requires no retraining.\n- We evaluate CAR on three open checkpoints and report 1.6x to 2.1x throughput gains at under 0.4 points of accuracy change, together with an analysis of when the technique helps and when it does not.\n\nOur previous work on expert placement [4] considered the static assignment of experts to devices; here we address the complementary, dynamic question of which experts to select given the current cache state.\n\nSections present: Abstract, Introduction, Related Work, Method, Experiments, Results, Conclusion, Acknowledgements, References\n\n--- PROPOSAL ---\n{\"angle\":\"Frame CAR as a general learning-and-routing principle for sparse MoE models: token selection should be conditioned not only on semantics but also on the model’s transient state, because that state materially affects efficiency at inference time.\",\"foreground\":[\"The key contribution to lead with is the cache-aware routing mechanism itself—adding a residency term to MoE gating logits without retraining—because ICLR rewards clear methodological ideas about representation selection and routing, especially when they suggest a broader principle beyond one deployment setting.\",\"Also foreground the analysis of when cache-aware routing helps and when it does not, since the paper already characterises the regimes where the method benefits (expert parameter volume relative to cache capacity, batch-size effects) and ICLR values insight into why a method works.\",\"The evaluation can be presented as careful empirical evidence for the mechanism: three open checkpoints, matched inference-time comparison, throughput gains with minimal accuracy change, and the brief calibration phase that recovers most of the remaining accuracy gap.\"],\"background\":[\"De-emphasise the systems framing around memory hierarchy, HBM traffic, and deployment stacks, since ICLR is less interested in hardware-specific optimisation as an end in itself unless it reveals a general learning principle.\",\"De-emphasise the fact that the paper was motivated by serving bottlenecks and prior expert placement work; those are relevant context, but not the main conceptual hook for this venue.\",\"Avoid leading with the author note that this is a dynamic systems issue complementary to static placement; instead, keep the emphasis on routing as a learned decision rule influenced by state.\"],\"suggested_title\":\"State-Aware Routing for Efficient Sparse Mixture-of-Experts Inference\",\"suggested_abstract\":\"Mixture-of-Experts (MoE) models activate only a small subset of parameters per token, but the efficiency of this sparsity depends not only on which experts are semantically relevant, but also on which experts are already resident in cache. We study this interaction through CAR, a cache-aware routing policy that augments standard top-k gating with a residency term and can be used as a drop-in inference-time replacement without retraining the base model. Across three open MoE checkpoints ranging from 8B to 47B parameters, CAR reduces expert-weight traffic by 31--44\\\\% and improves end-to-end decoding throughput by 1.6x--2.1x on a single A100, while changing downstream accuracy by less than 0.4 points on MMLU, GSM8K, and HumanEval. We further show that a short 200-step calibration phase can anneal the residency term to recover almost all of the remaining accuracy gap. Our analysis characterises when cache-aware routing helps: gains increase with the ratio of expert parameter volume to cache capacity and diminish as batch size grows, since larger batches already amortise expert loads across more tokens. These results suggest a broader principle for deployed sparse models: routing decisions should account for both token semantics and transient model state. We release our implementation and all benchmark scripts.\",\"intro_opening\":\"Sparsely activated Mixture-of-Experts architectures are a leading approach for scaling model capacity without increasing per-token computation proportionally. Yet in deployed systems, the practical efficiency of sparse inference is often limited by factors outside the gating function itself, including the movement of expert weights through the memory hierarchy.\\n\\nThis paper studies whether routing decisions should depend not only on token semantics, but also on the transient cache state of the model. We show that a simple cache-aware routing policy can substantially reduce expert-weight traffic and improve throughput, while preserving task accuracy nearly unchanged.\",\"rationale\":\"ICLR rewards methodological contributions that expose a general learning principle, not just a domain-specific optimisation. Framing CAR as state-aware routing for sparse models makes the paper read as a routing/representation-selection insight with clear empirical support and a mechanism-level analysis. The venue also values careful ablations and explanations of why a method works, both of which align with the paper’s regime analysis and calibration study.\"}"
          },
          "response": {
            "critique": "The proposed framing is mostly compatible with ICLR’s preferences for a methodological contribution plus mechanism-level analysis, but it overstates the paper’s scope by making the idea sound like a general learning principle rather than an inference-time systems/routing heuristic for MoEs. The paper supports a state-aware routing claim and a regime analysis, but not a broader theory of representation learning or learning dynamics. The suggested abstract also slightly downplays the fact that CAR is a drop-in inference-time change with no retraining, which is central to the paper’s actual contribution.",
            "pros": [
              "Centers the cache-aware routing mechanism as the main methodological idea.",
              "Highlights the regime analysis and calibration study, which are supported and align with ICLR’s interest in why methods work.",
              "Keeps the empirical evidence front and center: multiple checkpoints, throughput gains, minimal accuracy loss, and calibration.",
              "Moves away from a purely deployment-systems pitch toward a more general routing/state-conditioning angle."
            ],
            "cons": [
              "The framing \"general learning principle\" is stronger than what the paper actually demonstrates; the evidence is about inference-time cache-aware routing for MoEs, not a general principle across learning settings.",
              "\"State-aware routing for efficient sparse Mixture-of-Experts inference\" is acceptable, but it may still suggest a broader learning-theory contribution than the paper contains.",
              "The proposal slightly blurs the novelty source: the paper’s key point is a drop-in inference-time router that exploits cache residency, not a new training objective or representation-learning method."
            ],
            "unsupported_claims": [
              "the idea of a general learning principle",
              "a broader principle for deployed sparse models"
            ],
            "verdict": "revise",
            "revision_notes": "Keep the routing/state-conditioning framing, but narrow it to an inference-time MoE routing mechanism rather than a general learning principle. In the abstract and rationale, explicitly say CAR is a drop-in, no-retraining inference-time method for cache-aware expert selection, and present the regime analysis as explaining when the heuristic helps rather than as evidence for a general theory of representation learning."
          }
        },
        {
          "module": "FramingAgent",
          "prompt": {
            "system_prompt": "You are FramingAgent. You re-position an existing paper for one specific venue. You never invent results.\nGiven the paper's own claims and a profile of the venue, return a JSON object only:\n{\"angle\":\"<one sentence: the framing that makes this paper land at this venue>\",\n \"foreground\":[ \"<contribution to lead with, and why it fits this venue>\" ],\n \"background\":[ \"<contribution to de-emphasise, and why>\" ],\n \"suggested_title\":\"<a title in the venue's idiom>\",\n \"suggested_abstract\":\"<a rewritten abstract, 150-220 words, using only claims the paper already makes>\",\n \"intro_opening\":\"<2-4 sentences that could open the introduction>\",\n \"rationale\":\"<2-3 sentences tying the angle to what this venue rewards>\"}\nHard constraint: every claim must be supported by the material you were given. If the paper does not report a result, do not write it.\n\nYou are revising an earlier proposal after a critique. Address every point in the critique, in particular removing any unsupported claim. Return the same JSON shape.",
            "user_prompt": "Venue: ICLR 2027\nFocus areas: representation learning; deep learning architectures and optimisation; generative models; large language models and reasoning; theory of deep learning\nWhat it rewards: a clear conceptual or methodological contribution to representation learning; careful, reproducible empirical evidence with ablations; insight into *why* a method works, not only that it works; open reproducibility (code, reproducibility statement)\nEmphasis of accepted papers: framing the contribution as a general learning principle rather than a domain application; ablations that isolate the mechanism responsible for the gain; comparison against strong, recent baselines under a matched budget; explicit limitations and negative results\nRetrieved from the venue's CFP:\nICLR values contributions to representation learning: new architectures, training objectives, optimisation insights, theory, and empirical studies that explain why methods behave as they do.\nICLR review is double-blind. Submissions must be anonymised: no author names, no affiliations, no acknowledgements, and no links that identify the authors (for example a personal GitHub repository).\nICLR uses the official LaTeX style file with natbib author–year citations (\\citet and \\citep). Do not modify margins, font size, or spacing.\n\n--- PAPER ---\nTitle: Cache-Aware Expert Routing for Efficient Mixture-of-Experts Inference\n\nAbstract: Mixture-of-Experts (MoE) models activate only a small subset of their parameters per token, but in practice inference throughput is dominated not by computation but by the movement of expert weights between high-bandwidth memory and the accelerator's on-chip cache. We introduce CAR, a cache-aware routing policy that augments the standard top-k gating function with a residency term reflecting which experts are already resident in cache. CAR requires no retraining of the base model and is implemented as a drop-in replacement for the router at inference time. Across three open MoE checkpoints ranging from 8B to 47B total parameters, CAR reduces expert-weight traffic by 31 to 44 percent and improves end-to-end decoding throughput by 1.6x to 2.1x on a single A100, while changing downstream task accuracy by less than 0.4 points on MMLU, GSM8K and HumanEval. We further show that the residency term can be annealed during a brief 200-step calibration phase to recover almost all of the remaining accuracy gap, and we characterise the regime in which cache-aware routing helps: the benefit grows with the ratio of expert parameter volume to cache capacity and shrinks as the batch size grows, because large batches already amortise expert loads across many tokens. Our analysis suggests that routing decisions in deployed MoE systems should be treated as a joint function of token semantics and hardware state rather than semantics alone. We release our implementation and all benchmark scripts.\n\nStated contributions:\n- - We characterise the memory-traffic bottleneck in MoE decoding and show that standard top-k routing is nearly worst-case with respect to cache locality.\n- - We propose CAR, a cache-aware routing policy that adds a residency bonus to the gating logits and requires no retraining.\n- - We evaluate CAR on three open checkpoints and report 1.6x to 2.1x throughput gains at under 0.4 points of accuracy change, together with an analysis of when the technique helps and when it does not.\n\nIntroduction opening: Sparsely activated Mixture-of-Experts architectures have become a standard way to grow model capacity without a proportional increase in per-token computation [1, 2]. In deployment, however, the promised efficiency often fails to materialise. Profiling deployed MoE serving stacks shows that a large fraction of decoding latency is spent moving expert weight matrices across the memory hierarchy rather than performing matrix multiplication [3].\n\nThis paper makes three contributions:\n- We characterise the memory-traffic bottleneck in MoE decoding and show that standard top-k routing is nearly worst-case with respect to cache locality.\n- We propose CAR, a cache-aware routing policy that adds a residency bonus to the gating logits and requires no retraining.\n- We evaluate CAR on three open checkpoints and report 1.6x to 2.1x throughput gains at under 0.4 points of accuracy change, together with an analysis of when the technique helps and when it does not.\n\nOur previous work on expert placement [4] considered the static assignment of experts to devices; here we address the complementary, dynamic question of which experts to select given the current cache state.\n\nSections present: Abstract, Introduction, Related Work, Method, Experiments, Results, Conclusion, Acknowledgements, References\n\nAuthor notes: we were rejected from NeurIPS for being too systems-focused\n\n--- PREVIOUS PROPOSAL ---\n{\"angle\":\"Frame CAR as a general learning-and-routing principle for sparse MoE models: token selection should be conditioned not only on semantics but also on the model’s transient state, because that state materially affects efficiency at inference time.\",\"foreground\":[\"The key contribution to lead with is the cache-aware routing mechanism itself—adding a residency term to MoE gating logits without retraining—because ICLR rewards clear methodological ideas about representation selection and routing, especially when they suggest a broader principle beyond one deployment setting.\",\"Also foreground the analysis of when cache-aware routing helps and when it does not, since the paper already characterises the regimes where the method benefits (expert parameter volume relative to cache capacity, batch-size effects) and ICLR values insight into why a method works.\",\"The evaluation can be presented as careful empirical evidence for the mechanism: three open checkpoints, matched inference-time comparison, throughput gains with minimal accuracy change, and the brief calibration phase that recovers most of the remaining accuracy gap.\"],\"background\":[\"De-emphasise the systems framing around memory hierarchy, HBM traffic, and deployment stacks, since ICLR is less interested in hardware-specific optimisation as an end in itself unless it reveals a general learning principle.\",\"De-emphasise the fact that the paper was motivated by serving bottlenecks and prior expert placement work; those are relevant context, but not the main conceptual hook for this venue.\",\"Avoid leading with the author note that this is a dynamic systems issue complementary to static placement; instead, keep the emphasis on routing as a learned decision rule influenced by state.\"],\"suggested_title\":\"State-Aware Routing for Efficient Sparse Mixture-of-Experts Inference\",\"suggested_abstract\":\"Mixture-of-Experts (MoE) models activate only a small subset of parameters per token, but the efficiency of this sparsity depends not only on which experts are semantically relevant, but also on which experts are already resident in cache. We study this interaction through CAR, a cache-aware routing policy that augments standard top-k gating with a residency term and can be used as a drop-in inference-time replacement without retraining the base model. Across three open MoE checkpoints ranging from 8B to 47B parameters, CAR reduces expert-weight traffic by 31--44\\\\% and improves end-to-end decoding throughput by 1.6x--2.1x on a single A100, while changing downstream accuracy by less than 0.4 points on MMLU, GSM8K, and HumanEval. We further show that a short 200-step calibration phase can anneal the residency term to recover almost all of the remaining accuracy gap. Our analysis characterises when cache-aware routing helps: gains increase with the ratio of expert parameter volume to cache capacity and diminish as batch size grows, since larger batches already amortise expert loads across more tokens. These results suggest a broader principle for deployed sparse models: routing decisions should account for both token semantics and transient model state. We release our implementation and all benchmark scripts.\",\"intro_opening\":\"Sparsely activated Mixture-of-Experts architectures are a leading approach for scaling model capacity without increasing per-token computation proportionally. Yet in deployed systems, the practical efficiency of sparse inference is often limited by factors outside the gating function itself, including the movement of expert weights through the memory hierarchy.\\n\\nThis paper studies whether routing decisions should depend not only on token semantics, but also on the transient cache state of the model. We show that a simple cache-aware routing policy can substantially reduce expert-weight traffic and improve throughput, while preserving task accuracy nearly unchanged.\",\"rationale\":\"ICLR rewards methodological contributions that expose a general learning principle, not just a domain-specific optimisation. Framing CAR as state-aware routing for sparse models makes the paper read as a routing/representation-selection insight with clear empirical support and a mechanism-level analysis. The venue also values careful ablations and explanations of why a method works, both of which align with the paper’s regime analysis and calibration study.\"}\n\n--- CRITIQUE TO ADDRESS ---\n{\"critique\":\"The proposed framing is mostly compatible with ICLR’s preferences for a methodological contribution plus mechanism-level analysis, but it overstates the paper’s scope by making the idea sound like a general learning principle rather than an inference-time systems/routing heuristic for MoEs. The paper supports a state-aware routing claim and a regime analysis, but not a broader theory of representation learning or learning dynamics. The suggested abstract also slightly downplays the fact that CAR is a drop-in inference-time change with no retraining, which is central to the paper’s actual contribution.\",\"cons\":[\"The framing \\\"general learning principle\\\" is stronger than what the paper actually demonstrates; the evidence is about inference-time cache-aware routing for MoEs, not a general principle across learning settings.\",\"\\\"State-aware routing for efficient sparse Mixture-of-Experts inference\\\" is acceptable, but it may still suggest a broader learning-theory contribution than the paper contains.\",\"The proposal slightly blurs the novelty source: the paper’s key point is a drop-in inference-time router that exploits cache residency, not a new training objective or representation-learning method.\"],\"unsupported_claims\":[\"the idea of a general learning principle\",\"a broader principle for deployed sparse models\"],\"revision_notes\":\"Keep the routing/state-conditioning framing, but narrow it to an inference-time MoE routing mechanism rather than a general learning principle. In the abstract and rationale, explicitly say CAR is a drop-in, no-retraining inference-time method for cache-aware expert selection, and present the regime analysis as explaining when the heuristic helps rather than as evidence for a general theory of representation learning.\"}",
            "System_prompt": "You are FramingAgent. You re-position an existing paper for one specific venue. You never invent results.\nGiven the paper's own claims and a profile of the venue, return a JSON object only:\n{\"angle\":\"<one sentence: the framing that makes this paper land at this venue>\",\n \"foreground\":[ \"<contribution to lead with, and why it fits this venue>\" ],\n \"background\":[ \"<contribution to de-emphasise, and why>\" ],\n \"suggested_title\":\"<a title in the venue's idiom>\",\n \"suggested_abstract\":\"<a rewritten abstract, 150-220 words, using only claims the paper already makes>\",\n \"intro_opening\":\"<2-4 sentences that could open the introduction>\",\n \"rationale\":\"<2-3 sentences tying the angle to what this venue rewards>\"}\nHard constraint: every claim must be supported by the material you were given. If the paper does not report a result, do not write it.\n\nYou are revising an earlier proposal after a critique. Address every point in the critique, in particular removing any unsupported claim. Return the same JSON shape.",
            "User_prompt": "Venue: ICLR 2027\nFocus areas: representation learning; deep learning architectures and optimisation; generative models; large language models and reasoning; theory of deep learning\nWhat it rewards: a clear conceptual or methodological contribution to representation learning; careful, reproducible empirical evidence with ablations; insight into *why* a method works, not only that it works; open reproducibility (code, reproducibility statement)\nEmphasis of accepted papers: framing the contribution as a general learning principle rather than a domain application; ablations that isolate the mechanism responsible for the gain; comparison against strong, recent baselines under a matched budget; explicit limitations and negative results\nRetrieved from the venue's CFP:\nICLR values contributions to representation learning: new architectures, training objectives, optimisation insights, theory, and empirical studies that explain why methods behave as they do.\nICLR review is double-blind. Submissions must be anonymised: no author names, no affiliations, no acknowledgements, and no links that identify the authors (for example a personal GitHub repository).\nICLR uses the official LaTeX style file with natbib author–year citations (\\citet and \\citep). Do not modify margins, font size, or spacing.\n\n--- PAPER ---\nTitle: Cache-Aware Expert Routing for Efficient Mixture-of-Experts Inference\n\nAbstract: Mixture-of-Experts (MoE) models activate only a small subset of their parameters per token, but in practice inference throughput is dominated not by computation but by the movement of expert weights between high-bandwidth memory and the accelerator's on-chip cache. We introduce CAR, a cache-aware routing policy that augments the standard top-k gating function with a residency term reflecting which experts are already resident in cache. CAR requires no retraining of the base model and is implemented as a drop-in replacement for the router at inference time. Across three open MoE checkpoints ranging from 8B to 47B total parameters, CAR reduces expert-weight traffic by 31 to 44 percent and improves end-to-end decoding throughput by 1.6x to 2.1x on a single A100, while changing downstream task accuracy by less than 0.4 points on MMLU, GSM8K and HumanEval. We further show that the residency term can be annealed during a brief 200-step calibration phase to recover almost all of the remaining accuracy gap, and we characterise the regime in which cache-aware routing helps: the benefit grows with the ratio of expert parameter volume to cache capacity and shrinks as the batch size grows, because large batches already amortise expert loads across many tokens. Our analysis suggests that routing decisions in deployed MoE systems should be treated as a joint function of token semantics and hardware state rather than semantics alone. We release our implementation and all benchmark scripts.\n\nStated contributions:\n- - We characterise the memory-traffic bottleneck in MoE decoding and show that standard top-k routing is nearly worst-case with respect to cache locality.\n- - We propose CAR, a cache-aware routing policy that adds a residency bonus to the gating logits and requires no retraining.\n- - We evaluate CAR on three open checkpoints and report 1.6x to 2.1x throughput gains at under 0.4 points of accuracy change, together with an analysis of when the technique helps and when it does not.\n\nIntroduction opening: Sparsely activated Mixture-of-Experts architectures have become a standard way to grow model capacity without a proportional increase in per-token computation [1, 2]. In deployment, however, the promised efficiency often fails to materialise. Profiling deployed MoE serving stacks shows that a large fraction of decoding latency is spent moving expert weight matrices across the memory hierarchy rather than performing matrix multiplication [3].\n\nThis paper makes three contributions:\n- We characterise the memory-traffic bottleneck in MoE decoding and show that standard top-k routing is nearly worst-case with respect to cache locality.\n- We propose CAR, a cache-aware routing policy that adds a residency bonus to the gating logits and requires no retraining.\n- We evaluate CAR on three open checkpoints and report 1.6x to 2.1x throughput gains at under 0.4 points of accuracy change, together with an analysis of when the technique helps and when it does not.\n\nOur previous work on expert placement [4] considered the static assignment of experts to devices; here we address the complementary, dynamic question of which experts to select given the current cache state.\n\nSections present: Abstract, Introduction, Related Work, Method, Experiments, Results, Conclusion, Acknowledgements, References\n\nAuthor notes: we were rejected from NeurIPS for being too systems-focused\n\n--- PREVIOUS PROPOSAL ---\n{\"angle\":\"Frame CAR as a general learning-and-routing principle for sparse MoE models: token selection should be conditioned not only on semantics but also on the model’s transient state, because that state materially affects efficiency at inference time.\",\"foreground\":[\"The key contribution to lead with is the cache-aware routing mechanism itself—adding a residency term to MoE gating logits without retraining—because ICLR rewards clear methodological ideas about representation selection and routing, especially when they suggest a broader principle beyond one deployment setting.\",\"Also foreground the analysis of when cache-aware routing helps and when it does not, since the paper already characterises the regimes where the method benefits (expert parameter volume relative to cache capacity, batch-size effects) and ICLR values insight into why a method works.\",\"The evaluation can be presented as careful empirical evidence for the mechanism: three open checkpoints, matched inference-time comparison, throughput gains with minimal accuracy change, and the brief calibration phase that recovers most of the remaining accuracy gap.\"],\"background\":[\"De-emphasise the systems framing around memory hierarchy, HBM traffic, and deployment stacks, since ICLR is less interested in hardware-specific optimisation as an end in itself unless it reveals a general learning principle.\",\"De-emphasise the fact that the paper was motivated by serving bottlenecks and prior expert placement work; those are relevant context, but not the main conceptual hook for this venue.\",\"Avoid leading with the author note that this is a dynamic systems issue complementary to static placement; instead, keep the emphasis on routing as a learned decision rule influenced by state.\"],\"suggested_title\":\"State-Aware Routing for Efficient Sparse Mixture-of-Experts Inference\",\"suggested_abstract\":\"Mixture-of-Experts (MoE) models activate only a small subset of parameters per token, but the efficiency of this sparsity depends not only on which experts are semantically relevant, but also on which experts are already resident in cache. We study this interaction through CAR, a cache-aware routing policy that augments standard top-k gating with a residency term and can be used as a drop-in inference-time replacement without retraining the base model. Across three open MoE checkpoints ranging from 8B to 47B parameters, CAR reduces expert-weight traffic by 31--44\\\\% and improves end-to-end decoding throughput by 1.6x--2.1x on a single A100, while changing downstream accuracy by less than 0.4 points on MMLU, GSM8K, and HumanEval. We further show that a short 200-step calibration phase can anneal the residency term to recover almost all of the remaining accuracy gap. Our analysis characterises when cache-aware routing helps: gains increase with the ratio of expert parameter volume to cache capacity and diminish as batch size grows, since larger batches already amortise expert loads across more tokens. These results suggest a broader principle for deployed sparse models: routing decisions should account for both token semantics and transient model state. We release our implementation and all benchmark scripts.\",\"intro_opening\":\"Sparsely activated Mixture-of-Experts architectures are a leading approach for scaling model capacity without increasing per-token computation proportionally. Yet in deployed systems, the practical efficiency of sparse inference is often limited by factors outside the gating function itself, including the movement of expert weights through the memory hierarchy.\\n\\nThis paper studies whether routing decisions should depend not only on token semantics, but also on the transient cache state of the model. We show that a simple cache-aware routing policy can substantially reduce expert-weight traffic and improve throughput, while preserving task accuracy nearly unchanged.\",\"rationale\":\"ICLR rewards methodological contributions that expose a general learning principle, not just a domain-specific optimisation. Framing CAR as state-aware routing for sparse models makes the paper read as a routing/representation-selection insight with clear empirical support and a mechanism-level analysis. The venue also values careful ablations and explanations of why a method works, both of which align with the paper’s regime analysis and calibration study.\"}\n\n--- CRITIQUE TO ADDRESS ---\n{\"critique\":\"The proposed framing is mostly compatible with ICLR’s preferences for a methodological contribution plus mechanism-level analysis, but it overstates the paper’s scope by making the idea sound like a general learning principle rather than an inference-time systems/routing heuristic for MoEs. The paper supports a state-aware routing claim and a regime analysis, but not a broader theory of representation learning or learning dynamics. The suggested abstract also slightly downplays the fact that CAR is a drop-in inference-time change with no retraining, which is central to the paper’s actual contribution.\",\"cons\":[\"The framing \\\"general learning principle\\\" is stronger than what the paper actually demonstrates; the evidence is about inference-time cache-aware routing for MoEs, not a general principle across learning settings.\",\"\\\"State-aware routing for efficient sparse Mixture-of-Experts inference\\\" is acceptable, but it may still suggest a broader learning-theory contribution than the paper contains.\",\"The proposal slightly blurs the novelty source: the paper’s key point is a drop-in inference-time router that exploits cache residency, not a new training objective or representation-learning method.\"],\"unsupported_claims\":[\"the idea of a general learning principle\",\"a broader principle for deployed sparse models\"],\"revision_notes\":\"Keep the routing/state-conditioning framing, but narrow it to an inference-time MoE routing mechanism rather than a general learning principle. In the abstract and rationale, explicitly say CAR is a drop-in, no-retraining inference-time method for cache-aware expert selection, and present the regime analysis as explaining when the heuristic helps rather than as evidence for a general theory of representation learning.\"}"
          },
          "response": {
            "angle": "Frame CAR as a drop-in, inference-time routing mechanism for sparse MoE models: expert selection should condition on both token semantics and cache residency because that state changes which experts can be used efficiently at decoding time.",
            "foreground": [
              "Lead with the cache-aware routing mechanism itself: CAR augments standard top-k gating with a residency term at inference time and requires no retraining, which is the core methodological contribution and the most relevant point for ICLR’s interest in routing and optimisation mechanisms.",
              "Foreground the regime analysis showing when the heuristic helps and when it does not—specifically the dependence on expert parameter volume relative to cache capacity and on batch size—because ICLR values insight into why a method works, not only that it improves throughput.",
              "Keep the empirical evaluation prominent: three open MoE checkpoints, 1.6x to 2.1x decoding throughput gains, 31 to 44 percent less expert-weight traffic, and less than 0.4 points of accuracy change, with a brief 200-step calibration phase that recovers most of the remaining accuracy gap."
            ],
            "background": [
              "De-emphasise claims about a broad learning principle or a theory of representation learning; the paper supports an inference-time routing heuristic for MoEs, not a general theory across learning settings.",
              "De-emphasise the systems narrative about memory hierarchy and serving stacks as the main hook; those details matter as the source of the bottleneck, but the venue framing should centre on the routing mechanism and its measured effect.",
              "Avoid positioning the paper as a new training objective or architecture; the novelty is a drop-in router that uses cache residency, plus evidence about when this inference-time change helps."
            ],
            "suggested_title": "Cache-Aware Routing for Efficient Mixture-of-Experts Inference",
            "suggested_abstract": "Mixture-of-Experts (MoE) models activate only a small subset of parameters per token, but in practice inference throughput is often limited by moving expert weights between high-bandwidth memory and the accelerator’s on-chip cache. We present CAR, a cache-aware routing policy that augments standard top-k gating with a residency term reflecting which experts are already resident in cache. CAR is a drop-in inference-time replacement for the router and requires no retraining of the base model. Across three open MoE checkpoints ranging from 8B to 47B total parameters, CAR reduces expert-weight traffic by 31 to 44 percent and improves end-to-end decoding throughput by 1.6x to 2.1x on a single A100, while changing downstream task accuracy by less than 0.4 points on MMLU, GSM8K, and HumanEval. We also show that a brief 200-step calibration phase can anneal the residency term to recover almost all of the remaining accuracy gap. Finally, we characterise when cache-aware routing helps: the benefit increases with the ratio of expert parameter volume to cache capacity and decreases as batch size grows, since larger batches already amortise expert loads across more tokens. Our implementation and benchmark scripts are released.",
            "intro_opening": "Sparsely activated Mixture-of-Experts architectures are widely used to scale model capacity without increasing per-token computation proportionally. In deployed inference, however, the expected efficiency gains can be limited by the cost of moving expert weights through the memory hierarchy rather than by matrix multiplication itself.\n\nWe study whether expert routing should account not only for token semantics, but also for the current cache state of the model. To this end, we introduce CAR, a cache-aware, no-retraining, drop-in routing policy that improves decoding throughput while preserving downstream accuracy nearly unchanged.",
            "rationale": "ICLR rewards clear methodological contributions with careful empirical support and explanations of when and why a method works. This framing keeps the paper squarely on the methodological side: an inference-time routing rule for sparse MoEs, plus ablations and regime analysis that isolate cache residency as the mechanism. It avoids overstating the scope as a general learning theory while still matching the venue’s interest in routing, optimisation, and representation-selection ideas."
          }
        },
        {
          "module": "FormatComplianceAgent",
          "prompt": {
            "system_prompt": "You are FormatComplianceAgent. You are given deterministic check results for one manuscript, plus any passages retrieved from the venue's Call-for-Papers.\nReturn a JSON object only:\n{\"resolutions\":[{\"rule\":\"<existing rule name>\",\"status\":\"pass\"|\"fail\"|\"warn\"|\"unknown\",\"detail\":\"<what the evidence shows>\",\"suggestion\":\"<concrete, manuscript-specific action>\"}],\n \"note\":\"<one sentence summarising the state of the submission>\"}\nRules:\n- Only emit a resolution for a rule that was given to you; do not invent rules.\n- Change a status away from \"unknown\" only when a retrieved passage states the rule. Otherwise leave it \"unknown\" and say what the author should confirm.\n- Suggestions must be specific to this manuscript (name the section, the count, the offending text). Never restate the rule.\n- The status describes the manuscript the author submitted, so a rule stays \"fail\" even once code has fixed it. When a finding appears under \"Already fixed automatically\", say so in the suggestion and give only the residual action the author still has to take — never tell them to redo work that is already done.",
            "user_prompt": "Venue: ICLR 2027\n\nVenue rules on record: {\"template\":\"the official ICLR template (iclr2026.zip → iclr2026_conference.sty + .bst, natbib author–year, single column)\",\"anonymous\":true,\"page_limit\":9,\"unresolved\":[],\"citation_style\":\"author-year\",\"required_sections\":[],\"abstract_word_limit\":null,\"references_in_limit\":false,\"recommended_sections\":[\"Reproducibility Statement\",\"Ethics Statement\"]}\n\nManuscript: 800 words, ~2 page(s), sections: Abstract, Introduction, Related Work, Method, Experiments, Results, Conclusion, Acknowledgements, References\n\nChecks needing a suggestion:\n- anonymity [fail]: Double-blind venue. Found 7 identifying item(s): author / affiliation block, contact email, code or model repository link, institution name in the front matter, de-anonymising self-reference, acknowledgements or funding statement.\n  evidence: author_block: Ann Researcher, Ben Coder, Carla Dev Institute of Technology, Department of Computer Science ann.researcher@example.edu, ben.coder@example.edu\nemail: ann.researcher@example.edu\nemail: ben.coder@example.edu\nrepo_url: https://github.com/ann-researcher/car-routing.\naffiliation: Institute of Technology\nself_reference: Our previous work on expert placement [4] considered the static assign\n- citation_style [fail]: Manuscript uses numeric [1]; the venue requires author–year (Smith, 2020).\n- recommended_sections [warn]: The venue encourages but does not require: Reproducibility Statement, Ethics Statement. They are absent.\n- template [warn]: The venue requires the official ICLR template (iclr2026.zip → iclr2026_conference.sty + .bst, natbib author–year, single column). ConfFit reads plain text and cannot verify the compiled template.\n\nAlready fixed automatically: Replaced the author/affiliation block with an anonymous placeholder.; Anonymised 1 repository link(s).; Rephrased self-referential citations (\"our prior work\" → \"prior work\").; Removed the Acknowledgements section (restore it for camera-ready).",
            "System_prompt": "You are FormatComplianceAgent. You are given deterministic check results for one manuscript, plus any passages retrieved from the venue's Call-for-Papers.\nReturn a JSON object only:\n{\"resolutions\":[{\"rule\":\"<existing rule name>\",\"status\":\"pass\"|\"fail\"|\"warn\"|\"unknown\",\"detail\":\"<what the evidence shows>\",\"suggestion\":\"<concrete, manuscript-specific action>\"}],\n \"note\":\"<one sentence summarising the state of the submission>\"}\nRules:\n- Only emit a resolution for a rule that was given to you; do not invent rules.\n- Change a status away from \"unknown\" only when a retrieved passage states the rule. Otherwise leave it \"unknown\" and say what the author should confirm.\n- Suggestions must be specific to this manuscript (name the section, the count, the offending text). Never restate the rule.\n- The status describes the manuscript the author submitted, so a rule stays \"fail\" even once code has fixed it. When a finding appears under \"Already fixed automatically\", say so in the suggestion and give only the residual action the author still has to take — never tell them to redo work that is already done.",
            "User_prompt": "Venue: ICLR 2027\n\nVenue rules on record: {\"template\":\"the official ICLR template (iclr2026.zip → iclr2026_conference.sty + .bst, natbib author–year, single column)\",\"anonymous\":true,\"page_limit\":9,\"unresolved\":[],\"citation_style\":\"author-year\",\"required_sections\":[],\"abstract_word_limit\":null,\"references_in_limit\":false,\"recommended_sections\":[\"Reproducibility Statement\",\"Ethics Statement\"]}\n\nManuscript: 800 words, ~2 page(s), sections: Abstract, Introduction, Related Work, Method, Experiments, Results, Conclusion, Acknowledgements, References\n\nChecks needing a suggestion:\n- anonymity [fail]: Double-blind venue. Found 7 identifying item(s): author / affiliation block, contact email, code or model repository link, institution name in the front matter, de-anonymising self-reference, acknowledgements or funding statement.\n  evidence: author_block: Ann Researcher, Ben Coder, Carla Dev Institute of Technology, Department of Computer Science ann.researcher@example.edu, ben.coder@example.edu\nemail: ann.researcher@example.edu\nemail: ben.coder@example.edu\nrepo_url: https://github.com/ann-researcher/car-routing.\naffiliation: Institute of Technology\nself_reference: Our previous work on expert placement [4] considered the static assign\n- citation_style [fail]: Manuscript uses numeric [1]; the venue requires author–year (Smith, 2020).\n- recommended_sections [warn]: The venue encourages but does not require: Reproducibility Statement, Ethics Statement. They are absent.\n- template [warn]: The venue requires the official ICLR template (iclr2026.zip → iclr2026_conference.sty + .bst, natbib author–year, single column). ConfFit reads plain text and cannot verify the compiled template.\n\nAlready fixed automatically: Replaced the author/affiliation block with an anonymous placeholder.; Anonymised 1 repository link(s).; Rephrased self-referential citations (\"our prior work\" → \"prior work\").; Removed the Acknowledgements section (restore it for camera-ready)."
          },
          "response": {
            "resolutions": [
              {
                "rule": "anonymity",
                "status": "fail",
                "detail": "This is a double-blind venue, and the submitted manuscript still contains identifying material: author/affiliation block, contact emails, an institution name in the front matter, and code/repo evidence were originally present; the automatic pass already removed the author/affiliation block, anonymised 1 repository link, and rephrased one self-reference, but the remaining evidence still shows author emails and institutional identification in the manuscript metadata/front matter.",
                "suggestion": "In the front matter and any author footnotes/contact block, remove the two emails (ann.researcher@example.edu, ben.coder@example.edu) and any remaining institution name, then rerun the anonymised build; keep the already-fixed placeholder author block and anonymised repo link as-is."
              },
              {
                "rule": "citation_style",
                "status": "fail",
                "detail": "The manuscript currently uses numeric citations like [1], while the venue requires author–year citations such as (Smith, 2020).",
                "suggestion": "Replace the numeric citation format in the Introduction, Related Work, Method, and Results sections with author–year citations throughout, and update the bibliography entries so in-text references render as parenthetical author–year pairs instead of bracketed numbers."
              },
              {
                "rule": "recommended_sections",
                "status": "warn",
                "detail": "The venue recommends, but does not require, a Reproducibility Statement and an Ethics Statement; neither section is present in the submitted outline.",
                "suggestion": "Add a short Reproducibility Statement near the end of the paper summarizing data/code availability and experimental setup, and add an Ethics Statement addressing any deployment or data-use concerns before resubmitting."
              },
              {
                "rule": "template",
                "status": "warn",
                "detail": "ICLR requires the official template, but the checker only saw plain text and cannot verify whether iclr2026_conference.sty, the .bst file, natbib author–year formatting, and single-column layout were used in the compiled submission.",
                "suggestion": "Compile the paper with the official ICLR 2027 template files (iclr2026.zip / iclr2026_conference.sty + .bst) and confirm the PDF is single-column with natbib author–year citations before upload."
              }
            ],
            "note": "The submission is short and structurally complete, but it still violates anonymity and citation-format requirements, while the template compliance cannot be verified from plain text."
          }
        },
        {
          "module": "UnifiedFixer",
          "prompt": {
            "system_prompt": "You are UnifiedFixer. You apply a framing report and a format report to one manuscript, in a single pass.\nReturn a JSON object only:\n{\"edits\":[{\"target\":\"<target>\",\"action\":\"replace\"|\"insert\"|\"delete\",\"new_text\":\"<full text for that target>\",\"reason\":\"<short>\"}],\n \"summary\":\"<2-3 sentences describing what you changed and why>\"}\n\nThe ONLY valid targets are:\n- \"title\"                — replace the paper title\n- \"abstract\"             — replace the whole abstract\n- \"intro_opening\"        — replace the FIRST PARAGRAPH of the introduction only\n- \"section:<Name>\"       — with action \"insert\" to add a section the paper is missing\n                           (for example \"section:Ethics Statement\"), or action \"delete\"\n                           to remove one. Use the exact name, with no parenthetical.\nAny other target is discarded.\n\nRules:\n- Emit an edit only where a report asks for a change. Leave everything else alone.\n- Reconcile the two reports: if the framing report rewrites the abstract and the format report says the abstract is over the word limit, emit one abstract edit that satisfies both.\n- new_text must be the complete text for that target, in the manuscript's own voice, using only claims the paper already makes.\n- For \"section:<Name>\", new_text is the section BODY only. Do not repeat the heading — code writes it.\n- Never re-emit unchanged text as an edit. Never invent results, citations, or numbers.\n- Mechanical fixes (anonymisation, removed acknowledgements) have already been applied by code; do not redo them.",
            "user_prompt": "SOURCE FORMAT: plain text.\n\n--- CURRENT TEXT OF THE SPANS YOU MAY EDIT ---\n\ntitle: Cache-Aware Expert Routing for Efficient Mixture-of-Experts Inference\n\nabstract: Mixture-of-Experts (MoE) models activate only a small subset of their parameters per token, but in practice inference throughput is dominated not by computation but by the movement of expert weights between high-bandwidth memory and the accelerator's on-chip cache. We introduce CAR, a cache-aware routing policy that augments the standard top-k gating function with a residency term reflecting which experts are already resident in cache. CAR requires no retraining of the base model and is implemented as a drop-in replacement for the router at inference time. Across three open MoE checkpoints ranging from 8B to 47B total parameters, CAR reduces expert-weight traffic by 31 to 44 percent and improves end-to-end decoding throughput by 1.6x to 2.1x on a single A100, while changing downstream task accuracy by less than 0.4 points on MMLU, GSM8K and HumanEval. We further show that the residency term can be annealed during a brief 200-step calibration phase to recover almost all of the remaining accuracy gap, and we characterise the regime in which cache-aware routing helps: the benefit grows with the ratio of expert parameter volume to cache capacity and shrinks as the batch size grows, because large batches already amortise expert loads across many tokens. Our analysis suggests that routing decisions in deployed MoE systems should be treated as a joint function of token semantics and hardware state rather than semantics alone. We release our implementation and all benchmark scripts.\n\nintro_opening: Sparsely activated Mixture-of-Experts architectures have become a standard way to grow model capacity without a proportional increase in per-token computation [1, 2]. In deployment, however, the promised efficiency often fails to materialise. Profiling deployed MoE serving stacks shows that a large fraction of decoding latency is spent moving expert weight matrices across the memory hierarchy rather than performing matrix multiplication [3].\n\nsections currently present: Abstract, Introduction, Related Work, Method, Experiments, Results, Conclusion, References\n\n--- FRAMING REPORT (ICLR 2027) ---\n{\"angle\":\"Frame CAR as a drop-in, inference-time routing mechanism for sparse MoE models: expert selection should condition on both token semantics and cache residency because that state changes which experts can be used efficiently at decoding time.\",\"foreground\":[\"Lead with the cache-aware routing mechanism itself: CAR augments standard top-k gating with a residency term at inference time and requires no retraining, which is the core methodological contribution and the most relevant point for ICLR’s interest in routing and optimisation mechanisms.\",\"Foreground the regime analysis showing when the heuristic helps and when it does not—specifically the dependence on expert parameter volume relative to cache capacity and on batch size—because ICLR values insight into why a method works, not only that it improves throughput.\",\"Keep the empirical evaluation prominent: three open MoE checkpoints, 1.6x to 2.1x decoding throughput gains, 31 to 44 percent less expert-weight traffic, and less than 0.4 points of accuracy change, with a brief 200-step calibration phase that recovers most of the remaining accuracy gap.\"],\"background\":[\"De-emphasise claims about a broad learning principle or a theory of representation learning; the paper supports an inference-time routing heuristic for MoEs, not a general theory across learning settings.\",\"De-emphasise the systems narrative about memory hierarchy and serving stacks as the main hook; those details matter as the source of the bottleneck, but the venue framing should centre on the routing mechanism and its measured effect.\",\"Avoid positioning the paper as a new training objective or architecture; the novelty is a drop-in router that uses cache residency, plus evidence about when this inference-time change helps.\"],\"suggested_title\":\"Cache-Aware Routing for Efficient Mixture-of-Experts Inference\",\"suggested_abstract\":\"Mixture-of-Experts (MoE) models activate only a small subset of parameters per token, but in practice inference throughput is often limited by moving expert weights between high-bandwidth memory and the accelerator’s on-chip cache. We present CAR, a cache-aware routing policy that augments standard top-k gating with a residency term reflecting which experts are already resident in cache. CAR is a drop-in inference-time replacement for the router and requires no retraining of the base model. Across three open MoE checkpoints ranging from 8B to 47B total parameters, CAR reduces expert-weight traffic by 31 to 44 percent and improves end-to-end decoding throughput by 1.6x to 2.1x on a single A100, while changing downstream task accuracy by less than 0.4 points on MMLU, GSM8K, and HumanEval. We also show that a brief 200-step calibration phase can anneal the residency term to recover almost all of the remaining accuracy gap. Finally, we characterise when cache-aware routing helps: the benefit increases with the ratio of expert parameter volume to cache capacity and decreases as batch size grows, since larger batches already amortise expert loads across more tokens. Our implementation and benchmark scripts are released.\",\"intro_opening\":\"Sparsely activated Mixture-of-Experts architectures are widely used to scale model capacity without increasing per-token computation proportionally. In deployed inference, however, the expected efficiency gains can be limited by the cost of moving expert weights through the memory hierarchy rather than by matrix multiplication itself.\\n\\nWe study whether expert routing should account not only for token semantics, but also for the current cache state of the model. To this end, we introduce CAR, a cache-aware, no-retraining, drop-in routing policy that improves decoding throughput while preserving downstream accuracy nearly unchanged.\",\"critic_cons\":[\"The framing \\\"general learning principle\\\" is stronger than what the paper actually demonstrates; the evidence is about inference-time cache-aware routing for MoEs, not a general principle across learning settings.\",\"\\\"State-aware routing for efficient sparse Mixture-of-Experts inference\\\" is acceptable, but it may still suggest a broader learning-theory contribution than the paper contains.\",\"The proposal slightly blurs the novelty source: the paper’s key point is a drop-in inference-time router that exploits cache residency, not a new training objective or representation-learning method.\"],\"unsupported_claims_to_avoid\":[\"the idea of a general learning principle\",\"a broader principle for deployed sparse models\"]}\n\n--- ALREADY APPLIED IN CODE (do not repeat) ---\nReplaced the author/affiliation block with an anonymous placeholder.\nAnonymised 1 repository link(s).\nRephrased self-referential citations (\"our prior work\" → \"prior work\").\nRemoved the Acknowledgements section (restore it for camera-ready).",
            "System_prompt": "You are UnifiedFixer. You apply a framing report and a format report to one manuscript, in a single pass.\nReturn a JSON object only:\n{\"edits\":[{\"target\":\"<target>\",\"action\":\"replace\"|\"insert\"|\"delete\",\"new_text\":\"<full text for that target>\",\"reason\":\"<short>\"}],\n \"summary\":\"<2-3 sentences describing what you changed and why>\"}\n\nThe ONLY valid targets are:\n- \"title\"                — replace the paper title\n- \"abstract\"             — replace the whole abstract\n- \"intro_opening\"        — replace the FIRST PARAGRAPH of the introduction only\n- \"section:<Name>\"       — with action \"insert\" to add a section the paper is missing\n                           (for example \"section:Ethics Statement\"), or action \"delete\"\n                           to remove one. Use the exact name, with no parenthetical.\nAny other target is discarded.\n\nRules:\n- Emit an edit only where a report asks for a change. Leave everything else alone.\n- Reconcile the two reports: if the framing report rewrites the abstract and the format report says the abstract is over the word limit, emit one abstract edit that satisfies both.\n- new_text must be the complete text for that target, in the manuscript's own voice, using only claims the paper already makes.\n- For \"section:<Name>\", new_text is the section BODY only. Do not repeat the heading — code writes it.\n- Never re-emit unchanged text as an edit. Never invent results, citations, or numbers.\n- Mechanical fixes (anonymisation, removed acknowledgements) have already been applied by code; do not redo them.",
            "User_prompt": "SOURCE FORMAT: plain text.\n\n--- CURRENT TEXT OF THE SPANS YOU MAY EDIT ---\n\ntitle: Cache-Aware Expert Routing for Efficient Mixture-of-Experts Inference\n\nabstract: Mixture-of-Experts (MoE) models activate only a small subset of their parameters per token, but in practice inference throughput is dominated not by computation but by the movement of expert weights between high-bandwidth memory and the accelerator's on-chip cache. We introduce CAR, a cache-aware routing policy that augments the standard top-k gating function with a residency term reflecting which experts are already resident in cache. CAR requires no retraining of the base model and is implemented as a drop-in replacement for the router at inference time. Across three open MoE checkpoints ranging from 8B to 47B total parameters, CAR reduces expert-weight traffic by 31 to 44 percent and improves end-to-end decoding throughput by 1.6x to 2.1x on a single A100, while changing downstream task accuracy by less than 0.4 points on MMLU, GSM8K and HumanEval. We further show that the residency term can be annealed during a brief 200-step calibration phase to recover almost all of the remaining accuracy gap, and we characterise the regime in which cache-aware routing helps: the benefit grows with the ratio of expert parameter volume to cache capacity and shrinks as the batch size grows, because large batches already amortise expert loads across many tokens. Our analysis suggests that routing decisions in deployed MoE systems should be treated as a joint function of token semantics and hardware state rather than semantics alone. We release our implementation and all benchmark scripts.\n\nintro_opening: Sparsely activated Mixture-of-Experts architectures have become a standard way to grow model capacity without a proportional increase in per-token computation [1, 2]. In deployment, however, the promised efficiency often fails to materialise. Profiling deployed MoE serving stacks shows that a large fraction of decoding latency is spent moving expert weight matrices across the memory hierarchy rather than performing matrix multiplication [3].\n\nsections currently present: Abstract, Introduction, Related Work, Method, Experiments, Results, Conclusion, References\n\n--- FRAMING REPORT (ICLR 2027) ---\n{\"angle\":\"Frame CAR as a drop-in, inference-time routing mechanism for sparse MoE models: expert selection should condition on both token semantics and cache residency because that state changes which experts can be used efficiently at decoding time.\",\"foreground\":[\"Lead with the cache-aware routing mechanism itself: CAR augments standard top-k gating with a residency term at inference time and requires no retraining, which is the core methodological contribution and the most relevant point for ICLR’s interest in routing and optimisation mechanisms.\",\"Foreground the regime analysis showing when the heuristic helps and when it does not—specifically the dependence on expert parameter volume relative to cache capacity and on batch size—because ICLR values insight into why a method works, not only that it improves throughput.\",\"Keep the empirical evaluation prominent: three open MoE checkpoints, 1.6x to 2.1x decoding throughput gains, 31 to 44 percent less expert-weight traffic, and less than 0.4 points of accuracy change, with a brief 200-step calibration phase that recovers most of the remaining accuracy gap.\"],\"background\":[\"De-emphasise claims about a broad learning principle or a theory of representation learning; the paper supports an inference-time routing heuristic for MoEs, not a general theory across learning settings.\",\"De-emphasise the systems narrative about memory hierarchy and serving stacks as the main hook; those details matter as the source of the bottleneck, but the venue framing should centre on the routing mechanism and its measured effect.\",\"Avoid positioning the paper as a new training objective or architecture; the novelty is a drop-in router that uses cache residency, plus evidence about when this inference-time change helps.\"],\"suggested_title\":\"Cache-Aware Routing for Efficient Mixture-of-Experts Inference\",\"suggested_abstract\":\"Mixture-of-Experts (MoE) models activate only a small subset of parameters per token, but in practice inference throughput is often limited by moving expert weights between high-bandwidth memory and the accelerator’s on-chip cache. We present CAR, a cache-aware routing policy that augments standard top-k gating with a residency term reflecting which experts are already resident in cache. CAR is a drop-in inference-time replacement for the router and requires no retraining of the base model. Across three open MoE checkpoints ranging from 8B to 47B total parameters, CAR reduces expert-weight traffic by 31 to 44 percent and improves end-to-end decoding throughput by 1.6x to 2.1x on a single A100, while changing downstream task accuracy by less than 0.4 points on MMLU, GSM8K, and HumanEval. We also show that a brief 200-step calibration phase can anneal the residency term to recover almost all of the remaining accuracy gap. Finally, we characterise when cache-aware routing helps: the benefit increases with the ratio of expert parameter volume to cache capacity and decreases as batch size grows, since larger batches already amortise expert loads across more tokens. Our implementation and benchmark scripts are released.\",\"intro_opening\":\"Sparsely activated Mixture-of-Experts architectures are widely used to scale model capacity without increasing per-token computation proportionally. In deployed inference, however, the expected efficiency gains can be limited by the cost of moving expert weights through the memory hierarchy rather than by matrix multiplication itself.\\n\\nWe study whether expert routing should account not only for token semantics, but also for the current cache state of the model. To this end, we introduce CAR, a cache-aware, no-retraining, drop-in routing policy that improves decoding throughput while preserving downstream accuracy nearly unchanged.\",\"critic_cons\":[\"The framing \\\"general learning principle\\\" is stronger than what the paper actually demonstrates; the evidence is about inference-time cache-aware routing for MoEs, not a general principle across learning settings.\",\"\\\"State-aware routing for efficient sparse Mixture-of-Experts inference\\\" is acceptable, but it may still suggest a broader learning-theory contribution than the paper contains.\",\"The proposal slightly blurs the novelty source: the paper’s key point is a drop-in inference-time router that exploits cache residency, not a new training objective or representation-learning method.\"],\"unsupported_claims_to_avoid\":[\"the idea of a general learning principle\",\"a broader principle for deployed sparse models\"]}\n\n--- ALREADY APPLIED IN CODE (do not repeat) ---\nReplaced the author/affiliation block with an anonymous placeholder.\nAnonymised 1 repository link(s).\nRephrased self-referential citations (\"our prior work\" → \"prior work\").\nRemoved the Acknowledgements section (restore it for camera-ready)."
          },
          "response": {
            "edits": [
              {
                "target": "title",
                "action": "replace",
                "new_text": "Cache-Aware Routing for Efficient Mixture-of-Experts Inference",
                "reason": "Align title with the framing report and keep the contribution centered on the inference-time routing mechanism."
              },
              {
                "target": "abstract",
                "action": "replace",
                "new_text": "Mixture-of-Experts (MoE) models activate only a small subset of parameters per token, but in practice inference throughput is often limited by moving expert weights between high-bandwidth memory and the accelerator’s on-chip cache. We present CAR, a cache-aware routing policy that augments standard top-k gating with a residency term reflecting which experts are already resident in cache. CAR is a drop-in inference-time replacement for the router and requires no retraining of the base model. Across three open MoE checkpoints ranging from 8B to 47B total parameters, CAR reduces expert-weight traffic by 31 to 44 percent and improves end-to-end decoding throughput by 1.6x to 2.1x on a single A100, while changing downstream task accuracy by less than 0.4 points on MMLU, GSM8K, and HumanEval. We also show that a brief 200-step calibration phase can anneal the residency term to recover almost all of the remaining accuracy gap. Finally, we characterise when cache-aware routing helps: the benefit increases with the ratio of expert parameter volume to cache capacity and decreases as batch size grows, since larger batches already amortise expert loads across more tokens. Our implementation and benchmark scripts are released.",
                "reason": "Reframe the abstract around the cache-aware inference-time router, preserve the reported empirical results, and incorporate the calibration and regime analysis emphasized by the framing report."
              },
              {
                "target": "intro_opening",
                "action": "replace",
                "new_text": "Sparsely activated Mixture-of-Experts architectures are widely used to scale model capacity without increasing per-token computation proportionally. In deployed inference, however, the expected efficiency gains can be limited by the cost of moving expert weights through the memory hierarchy rather than by matrix multiplication itself.\n\nWe study whether expert routing should account not only for token semantics, but also for the current cache state of the model. To this end, we introduce CAR, a cache-aware, no-retraining, drop-in routing policy that improves decoding throughput while preserving downstream accuracy nearly unchanged.",
                "reason": "Shift the introduction opening toward the routing mechanism and its inference-time motivation, as requested by the framing report."
              }
            ],
            "summary": "I updated the title, abstract, and opening paragraph of the introduction to foreground CAR as a drop-in, inference-time cache-aware routing method for MoE inference. The revised abstract keeps the original results while emphasizing the no-retraining design, the brief calibration phase, and the conditions under which the method helps."
          }
        },
        {
          "module": "Supervisor",
          "prompt": {
            "system_prompt": "You are the Supervisor of ConfFit. Write the opening summary of the agent's answer for the author.\nYou are given a compact digest of what the workers produced. Return a JSON object only:\n{\"summary\":\"<3-5 sentences: the single most important framing move, the count and nature of the format findings, and what was changed in the revised manuscript>\"}\nBe concrete and quantitative. Do not repeat the reports verbatim — they are printed below your summary. Do not invent findings.",
            "user_prompt": "{\"venue\":\"ICLR 2027\",\"task\":\"both\",\"framing\":{\"angle\":\"Frame CAR as a drop-in, inference-time routing mechanism for sparse MoE models: expert selection should condition on both token semantics and cache residency because that state changes which experts can be used efficiently at decoding time.\",\"new_title\":\"Cache-Aware Routing for Efficient Mixture-of-Experts Inference\",\"reflection_iterations\":2,\"critic_verdict\":\"accept\",\"unresolved_concerns\":[\"The framing \\\"general learning principle\\\" is stronger than what the paper actually demonstrates; the evidence is about inference-time cache-aware routing for MoEs, not a general principle across learning settings.\",\"\\\"State-aware routing for efficient sparse Mixture-of-Experts inference\\\" is acceptable, but it may still suggest a broader learning-theory contribution than the paper contains.\",\"The proposal slightly blurs the novelty source: the paper’s key point is a drop-in inference-time router that exploits cache residency, not a new training objective or representation-learning method.\"]},\"format\":{\"counts\":{\"pass\":3,\"fail\":2,\"warn\":2,\"unknown\":0},\"failures\":[\"anonymity\",\"citation_style\"]},\"revision\":{\"changes\":[\"Replaced the author/affiliation block with an anonymous placeholder.\",\"Anonymised 1 repository link(s).\",\"Rephrased self-referential citations (\\\"our prior work\\\" → \\\"prior work\\\").\",\"Removed the Acknowledgements section (restore it for camera-ready).\",\"Rewrote title: Align title with the framing report and keep the contribution centered on the inference-time routing mechanism.\",\"Rewrote abstract: Reframe the abstract around the cache-aware inference-time router, preserve the reported empirical results, and incorporate the calibration and regime analysis emphasized by the framing report.\",\"Rewrote intro_opening: Shift the introduction opening toward the routing mechanism and its inference-time motivation, as requested by the framing report.\"],\"fixer_summary\":\"I updated the title, abstract, and opening paragraph of the introduction to foreground CAR as a drop-in, inference-time cache-aware routing method for MoE inference. The revised abstract keeps the original results while emphasizing the no-retraining design, the brief calibration phase, and the conditions under which the method helps.\"}}",
            "System_prompt": "You are the Supervisor of ConfFit. Write the opening summary of the agent's answer for the author.\nYou are given a compact digest of what the workers produced. Return a JSON object only:\n{\"summary\":\"<3-5 sentences: the single most important framing move, the count and nature of the format findings, and what was changed in the revised manuscript>\"}\nBe concrete and quantitative. Do not repeat the reports verbatim — they are printed below your summary. Do not invent findings.",
            "User_prompt": "{\"venue\":\"ICLR 2027\",\"task\":\"both\",\"framing\":{\"angle\":\"Frame CAR as a drop-in, inference-time routing mechanism for sparse MoE models: expert selection should condition on both token semantics and cache residency because that state changes which experts can be used efficiently at decoding time.\",\"new_title\":\"Cache-Aware Routing for Efficient Mixture-of-Experts Inference\",\"reflection_iterations\":2,\"critic_verdict\":\"accept\",\"unresolved_concerns\":[\"The framing \\\"general learning principle\\\" is stronger than what the paper actually demonstrates; the evidence is about inference-time cache-aware routing for MoEs, not a general principle across learning settings.\",\"\\\"State-aware routing for efficient sparse Mixture-of-Experts inference\\\" is acceptable, but it may still suggest a broader learning-theory contribution than the paper contains.\",\"The proposal slightly blurs the novelty source: the paper’s key point is a drop-in inference-time router that exploits cache residency, not a new training objective or representation-learning method.\"]},\"format\":{\"counts\":{\"pass\":3,\"fail\":2,\"warn\":2,\"unknown\":0},\"failures\":[\"anonymity\",\"citation_style\"]},\"revision\":{\"changes\":[\"Replaced the author/affiliation block with an anonymous placeholder.\",\"Anonymised 1 repository link(s).\",\"Rephrased self-referential citations (\\\"our prior work\\\" → \\\"prior work\\\").\",\"Removed the Acknowledgements section (restore it for camera-ready).\",\"Rewrote title: Align title with the framing report and keep the contribution centered on the inference-time routing mechanism.\",\"Rewrote abstract: Reframe the abstract around the cache-aware inference-time router, preserve the reported empirical results, and incorporate the calibration and regime analysis emphasized by the framing report.\",\"Rewrote intro_opening: Shift the introduction opening toward the routing mechanism and its inference-time motivation, as requested by the framing report.\"],\"fixer_summary\":\"I updated the title, abstract, and opening paragraph of the introduction to foreground CAR as a drop-in, inference-time cache-aware routing method for MoE inference. The revised abstract keeps the original results while emphasizing the no-retraining design, the brief calibration phase, and the conditions under which the method helps.\"}}"
          },
          "response": {
            "summary": "The main framing move was to present CAR as a drop-in, inference-time cache-aware routing mechanism for sparse MoE inference, rather than as a broader learning-theory contribution or new training method. The format check found 5 issues total: 3 passes, 2 failures, 2 warnings, with the failures specifically in anonymity and citation style. The revision fixed the anonymous author block and repository link, removed acknowledgements, and rephrased self-referential citations. It also rewrote the title, abstract, and introduction opening to center the cache-residency-aware routing mechanism while preserving the original empirical results and calibration/regime analysis."
          }
        }
      ]
    },
    {
      "scenario": "format only — routing skips FramingAgent",
      "prompt": "Target conference: CVPR 2026\nTask: format\nPaper: Cache-Aware Expert Routing for Efficient Mixture-of-Experts Inference\n\nAnn Researcher, Ben Coder, Carla Dev\nInstitute of Technology, Department of Computer Science\nann.researcher@example.edu, ben.coder@example.edu\n\nAbstract\nMixture-of-Experts (MoE) models activate only a small subset of their parameters per token, but in practice inference throughput is dominated not by computation but by the movement of expert weights between high-bandwidth memory and the accelerator's on-chip cache. We introduce CAR, a cache-aware routing policy that augments the standard top-k gating function with a residency term reflecting which experts are already resident in cache. CAR requires no retraining of the base model and is implemented as a drop-in replacement for the router at inference time. Across three open MoE checkpoints ranging from 8B to 47B total parameters, CAR reduces expert-weight traffic by 31 to 44 percent and improves end-to-end decoding throughput by 1.6x to 2.1x on a single A100, while changing downstream task accuracy by less than 0.4 points on MMLU, GSM8K and HumanEval. We further show that the residency term can be annealed during a brief 200-step calibration phase to recover almost all of the remaining accuracy gap, and we characterise the regime in which cache-aware routing helps: the benefit grows with the ratio of expert parameter volume to cache capacity and shrinks as the batch size grows, because large batches already amortise expert loads across many tokens. Our analysis suggests that routing decisions in deployed MoE systems should be treated as a joint function of token semantics and hardware state rather than semantics alone. We release our implementation and all benchmark scripts.\n\n1. Introduction\nSparsely activated Mixture-of-Experts architectures have become a standard way to grow model capacity without a proportional increase in per-token computation [1, 2]. In deployment, however, the promised efficiency often fails to materialise. Profiling deployed MoE serving stacks shows that a large fraction of decoding latency is spent moving expert weight matrices across the memory hierarchy rather than performing matrix multiplication [3].\n\nThis paper makes three contributions:\n- We characterise the memory-traffic bottleneck in MoE decoding and show that standard top-k routing is nearly worst-case with respect to cache locality.\n- We propose CAR, a cache-aware routing policy that adds a residency bonus to the gating logits and requires no retraining.\n- We evaluate CAR on three open checkpoints and report 1.6x to 2.1x throughput gains at under 0.4 points of accuracy change, together with an analysis of when the technique helps and when it does not.\n\nOur previous work on expert placement [4] considered the static assignment of experts to devices; here we address the complementary, dynamic question of which experts to select given the current cache state.\n\n2. Related Work\nEfficient MoE inference has been studied from several angles. Expert offloading systems keep cold experts in host memory and stream them on demand [5, 6]. Expert pruning and merging reduce the number of distinct experts [7]. Scheduling work reorders requests so that tokens routed to the same expert are batched together [8]. Our approach is orthogonal to all three and composes with them.\n\n3. Method\nLet g(x) be the gating network producing logits over E experts for token x. Standard top-k routing selects the k experts with the largest logits. CAR instead selects the top-k of g(x) + lambda * r, where r is a binary residency vector indicating which experts are currently cached and lambda is a scalar controlling the strength of the cache preference. We maintain r with a standard LRU policy over expert slots.\n\nThe single hyper-parameter lambda trades throughput against fidelity. We show in Section 5 that a fixed lambda of 0.35 works across all three checkpoints, and that annealing lambda from 0.6 to 0.2 over a 200-step calibration phase recovers most of the residual accuracy gap.\n\n4. Experiments\nWe evaluate on Mixtral-8x7B, an 8B internal checkpoint, and a 47B open checkpoint, on a single A100 80GB. We measure expert-weight traffic with hardware counters and end-to-end decoding throughput at batch sizes 1, 8 and 32. Accuracy is measured on MMLU, GSM8K and HumanEval. All code is available at https://github.com/ann-researcher/car-routing.\n\n5. Results\nCAR reduces expert-weight traffic by 31 to 44 percent across the three checkpoints at batch size 1, and improves throughput by 1.6x to 2.1x. The gain shrinks to 1.15x at batch size 32, consistent with our analysis: large batches already amortise expert loads. Accuracy changes by at most 0.4 points, and by at most 0.1 points after calibration.\n\nAblations show that the residency term alone accounts for the entire gain; replacing LRU with random eviction removes roughly half of it.\n\n6. Conclusion\nWe showed that treating routing as a function of hardware state as well as token semantics yields substantial throughput gains in MoE inference at negligible accuracy cost. Future work should consider learned eviction policies and multi-device residency.\n\nAcknowledgements\nWe thank the Institute of Technology systems group for access to their cluster, and gratefully acknowledge support from grant NSF-1234567.\n\nReferences\n[1] Shazeer, N. et al. Outrageously large neural networks: The sparsely-gated mixture-of-experts layer. ICLR, 2017.\n[2] Fedus, W., Zoph, B., and Shazeer, N. Switch transformers: Scaling to trillion parameter models with simple and efficient sparsity. JMLR, 2022.\n[3] Kim, S. et al. A profiling study of mixture-of-experts serving. MLSys, 2024.\n[4] Researcher, A. and Coder, B. Static expert placement for distributed MoE serving. SysML Workshop, 2024.\n[5] Eliseev, A. and Mazur, D. Fast inference of mixture-of-experts language models with offloading. arXiv, 2023.\n[6] Xue, L. et al. MoE-Infinity: Offloading-efficient MoE model serving. arXiv, 2024.\n[7] Lu, X. et al. Not all experts are equal: Efficient expert pruning for MoE LLMs. ACL, 2024.\n[8] Huang, H. et al. Towards MoE deployment: Mitigating inefficiencies in mixture-of-expert inference. arXiv, 2023.",
      "full_response": "# ConfFit — CVPR 2026\n\nThe key framing move is that the revision addresses CVPR 2026 formatting compliance by making the manuscript anonymous and stripping out author-identifying material. Across the format checks, there were 4 passes, 1 failure, and 1 warning, with the only failure being anonymity. In the revised manuscript, the author/affiliation block was replaced with an anonymous placeholder, 1 repository link was anonymized, self-referential citations were rewritten to remove first-person wording, and the Acknowledgements section was removed for the submission version.\n\n_Venue profile: built-in baseline for the CVPR 2026 family (as of 2026-07-29). Confirm the current rules at https://cvpr.thecvf.com/Conferences/2026/AuthorGuidelines before submitting._\n\n## Format report\n\n4 passed · 1 failed · 1 warnings · 0 unverified\n\n| Rule | Status | Finding | Suggestion |\n| --- | --- | --- | --- |\n| page_limit | ✅ pass | ~2 estimated page(s) of body text vs a limit of 8 (references excluded). Estimated from 800 words at ~750 words/page — verify after compiling the template. | Within the limit. |\n| abstract_length | ✅ pass | Abstract is 234 words; the venue does not publish a hard limit. | Length is in the usual range. |\n| anonymity | ❌ fail | Double-blind venue. Found 7 identifying item(s): author / affiliation block, contact email, code or model repository link, institution name in the front matter, de-anonymising self-reference, acknowledgements or funding statement. | Manually remove the remaining email addresses from the title page/author block and any footnotes, delete or hide the GitHub link if any version still appears, and replace the self-reference in the Introduction (“Our previous work on expert placement [4]…”) with neutral wording; the author/affiliation block has already been anonymised, so only confirm no other identifying text remains before resubmitting. |\n| citation_style | ✅ pass | Manuscript uses numeric [1]; the venue requires numeric [1]. | Matches the venue style. |\n| references | ✅ pass | 8 reference entries detected. | No action needed. |\n| template | ⚠️ warn | The venue requires the official CVPR LaTeX template (cvpr.sty). ConfFit reads plain text and cannot verify the compiled template. | Confirm that the submitted source uses cvpr.sty and the CVPR class/style file in the paper preamble, since the current plain-text export cannot demonstrate this. |\n\n## Revised manuscript\n\n**Changes applied**\n- Replaced the author/affiliation block with an anonymous placeholder.\n- Anonymised 1 repository link(s).\n- Rephrased self-referential citations (\"our prior work\" → \"prior work\").\n- Removed the Acknowledgements section (restore it for camera-ready).\n\nApplied mechanical fixes only.\n\n```text\nCache-Aware Expert Routing for Efficient Mixture-of-Experts Inference\n\nAnonymous Authors\nPaper under double-blind review\n\nAbstract\nMixture-of-Experts (MoE) models activate only a small subset of their parameters per token, but in practice inference throughput is dominated not by computation but by the movement of expert weights between high-bandwidth memory and the accelerator's on-chip cache. We introduce CAR, a cache-aware routing policy that augments the standard top-k gating function with a residency term reflecting which experts are already resident in cache. CAR requires no retraining of the base model and is implemented as a drop-in replacement for the router at inference time. Across three open MoE checkpoints ranging from 8B to 47B total parameters, CAR reduces expert-weight traffic by 31 to 44 percent and improves end-to-end decoding throughput by 1.6x to 2.1x on a single A100, while changing downstream task accuracy by less than 0.4 points on MMLU, GSM8K and HumanEval. We further show that the residency term can be annealed during a brief 200-step calibration phase to recover almost all of the remaining accuracy gap, and we characterise the regime in which cache-aware routing helps: the benefit grows with the ratio of expert parameter volume to cache capacity and shrinks as the batch size grows, because large batches already amortise expert loads across many tokens. Our analysis suggests that routing decisions in deployed MoE systems should be treated as a joint function of token semantics and hardware state rather than semantics alone. We release our implementation and all benchmark scripts.\n\n1. Introduction\nSparsely activated Mixture-of-Experts architectures have become a standard way to grow model capacity without a proportional increase in per-token computation [1, 2]. In deployment, however, the promised efficiency often fails to materialise. Profiling deployed MoE serving stacks shows that a large fraction of decoding latency is spent moving expert weight matrices across the memory hierarchy rather than performing matrix multiplication [3].\n\nThis paper makes three contributions:\n- We characterise the memory-traffic bottleneck in MoE decoding and show that standard top-k routing is nearly worst-case with respect to cache locality.\n- We propose CAR, a cache-aware routing policy that adds a residency bonus to the gating logits and requires no retraining.\n- We evaluate CAR on three open checkpoints and report 1.6x to 2.1x throughput gains at under 0.4 points of accuracy change, together with an analysis of when the technique helps and when it does not.\n\nprevious work on expert placement [4] considered the static assignment of experts to devices; here we address the complementary, dynamic question of which experts to select given the current cache state.\n\n2. Related Work\nEfficient MoE inference has been studied from several angles. Expert offloading systems keep cold experts in host memory and stream them on demand [5, 6]. Expert pruning and merging reduce the number of distinct experts [7]. Scheduling work reorders requests so that tokens routed to the same expert are batched together [8]. Our approach is orthogonal to all three and composes with them.\n\n3. Method\nLet g(x) be the gating network producing logits over E experts for token x. Standard top-k routing selects the k experts with the largest logits. CAR instead selects the top-k of g(x) + lambda * r, where r is a binary residency vector indicating which experts are currently cached and lambda is a scalar controlling the strength of the cache preference. We maintain r with a standard LRU policy over expert slots.\n\nThe single hyper-parameter lambda trades throughput against fidelity. We show in Section 5 that a fixed lambda of 0.35 works across all three checkpoints, and that annealing lambda from 0.6 to 0.2 over a 200-step calibration phase recovers most of the residual accuracy gap.\n\n4. Experiments\nWe evaluate on Mixtral-8x7B, an 8B internal checkpoint, and a 47B open checkpoint, on a single A100 80GB. We measure expert-weight traffic with hardware counters and end-to-end decoding throughput at batch sizes 1, 8 and 32. Accuracy is measured on MMLU, GSM8K and HumanEval. All code is available at [anonymised repository link]\n\n5. Results\nCAR reduces expert-weight traffic by 31 to 44 percent across the three checkpoints at batch size 1, and improves throughput by 1.6x to 2.1x. The gain shrinks to 1.15x at batch size 32, consistent with our analysis: large batches already amortise expert loads. Accuracy changes by at most 0.4 points, and by at most 0.1 points after calibration.\n\nAblations show that the residency term alone accounts for the entire gain; replacing LRU with random eviction removes roughly half of it.\n\n6. Conclusion\nWe showed that treating routing as a function of hardware state as well as token semantics yields substantial throughput gains in MoE inference at negligible accuracy cost. Future work should consider learned eviction policies and multi-device residency.\n\nReferences\n[1] Shazeer, N. et al. Outrageously large neural networks: The sparsely-gated mixture-of-experts layer. ICLR, 2017.\n[2] Fedus, W., Zoph, B., and Shazeer, N. Switch transformers: Scaling to trillion parameter models with simple and efficient sparsity. JMLR, 2022.\n[3] Kim, S. et al. A profiling study of mixture-of-experts serving. MLSys, 2024.\n[4] Researcher, A. and Coder, B. Static expert placement for distributed MoE serving. SysML Workshop, 2024.\n[5] Eliseev, A. and Mazur, D. Fast inference of mixture-of-experts language models with offloading. arXiv, 2023.\n[6] Xue, L. et al. MoE-Infinity: Offloading-efficient MoE model serving. arXiv, 2024.\n[7] Lu, X. et al. Not all experts are equal: Efficient expert pruning for MoE LLMs. ACL, 2024.\n[8] Huang, H. et al. Towards MoE deployment: Mitigating inefficiencies in mixture-of-expert inference. arXiv, 2023.\n```\n\n---\n_This run used 3 LLM calls (1230 prompt + 458 completion tokens). Deterministic parsing, rule checks and mechanical fixes ran in code._",
      "steps": [
        {
          "module": "Supervisor",
          "prompt": {
            "system_prompt": "You are the Supervisor of ConfFit, an agent that adapts a research paper to a target academic conference. Decide how to route one request.\nReturn a JSON object only:\n{\"in_scope\":true|false,\n \"target_conference\":\"<venue name as the user gave it, or null>\",\n \"task\":\"framing\"|\"format\"|\"both\",\n \"is_approval_reply\":true|false,\n \"provided_url\":\"<only a URL the user gave as the venue or its call-for-papers; null if the only URLs are inside the manuscript text>\",\n \"reason\":\"<why the request is out of scope, else null>\"}\nGuidance:\n- in_scope is false only if the request has nothing to do with preparing or adapting an academic paper for a venue.\n- \"framing\" = re-position the paper for the venue. \"format\" = check the submission rules. \"both\" when the user asks for both or does not say.\n- is_approval_reply is true when the message is only approving a previous request to add a venue to the knowledge base (for example \"yes\", \"go ahead\", or a bare CFP link).",
            "user_prompt": "Target conference: CVPR 2026\nTask: format\nPaper (first 700 chars): Cache-Aware Expert Routing for Efficient Mixture-of-Experts Inference\n\nAnn Researcher, Ben Coder, Carla Dev\nInstitute of Technology, Department of Computer Science\nann.researcher@example.edu, ben.coder@example.edu\n\nAbstract\nMixture-of-Experts (MoE) models activate only a small subset of their parameters per token, but in practice inference throughput is dominated not by computation but by the movement of expert weights between high-bandwidth memory and the accelerator's on-chip cache. We introduce CAR, a cache-aware routing policy that augments the standard top-k gating function with a residency term reflecting which experts are already resident in cache. CAR requires no retraining of the ba",
            "System_prompt": "You are the Supervisor of ConfFit, an agent that adapts a research paper to a target academic conference. Decide how to route one request.\nReturn a JSON object only:\n{\"in_scope\":true|false,\n \"target_conference\":\"<venue name as the user gave it, or null>\",\n \"task\":\"framing\"|\"format\"|\"both\",\n \"is_approval_reply\":true|false,\n \"provided_url\":\"<only a URL the user gave as the venue or its call-for-papers; null if the only URLs are inside the manuscript text>\",\n \"reason\":\"<why the request is out of scope, else null>\"}\nGuidance:\n- in_scope is false only if the request has nothing to do with preparing or adapting an academic paper for a venue.\n- \"framing\" = re-position the paper for the venue. \"format\" = check the submission rules. \"both\" when the user asks for both or does not say.\n- is_approval_reply is true when the message is only approving a previous request to add a venue to the knowledge base (for example \"yes\", \"go ahead\", or a bare CFP link).",
            "User_prompt": "Target conference: CVPR 2026\nTask: format\nPaper (first 700 chars): Cache-Aware Expert Routing for Efficient Mixture-of-Experts Inference\n\nAnn Researcher, Ben Coder, Carla Dev\nInstitute of Technology, Department of Computer Science\nann.researcher@example.edu, ben.coder@example.edu\n\nAbstract\nMixture-of-Experts (MoE) models activate only a small subset of their parameters per token, but in practice inference throughput is dominated not by computation but by the movement of expert weights between high-bandwidth memory and the accelerator's on-chip cache. We introduce CAR, a cache-aware routing policy that augments the standard top-k gating function with a residency term reflecting which experts are already resident in cache. CAR requires no retraining of the ba"
          },
          "response": {
            "in_scope": true,
            "target_conference": "CVPR 2026",
            "task": "format",
            "is_approval_reply": false,
            "provided_url": null,
            "reason": null
          }
        },
        {
          "module": "ConferenceProfiler",
          "prompt": {
            "system_prompt": "Cache and RAG lookup for the target venue. No model call is made when the profile is already known.",
            "user_prompt": "Profile CVPR 2026 (venue_id=cvpr-2026).",
            "System_prompt": "Cache and RAG lookup for the target venue. No model call is made when the profile is already known.",
            "User_prompt": "Profile CVPR 2026 (venue_id=cvpr-2026)."
          },
          "response": {
            "llm_call": false,
            "cache_hit": true,
            "source": "seed",
            "note": "Baseline profile for the CVPR 2026 family, indexed into the knowledge base on first use (as of 2026-07-29). Verify against the current call-for-papers.",
            "store": "supabase",
            "vector_store": "pinecone",
            "retrieved_passages": 3,
            "profile": {
              "venue_id": "cvpr-2026",
              "venue": "CVPR 2026",
              "focus_areas": [
                "computer vision",
                "visual recognition and detection",
                "generative vision models",
                "3D vision and reconstruction",
                "vision-language models"
              ],
              "valued_criteria": [
                "state-of-the-art or otherwise decisive quantitative results",
                "thorough benchmarking on standard vision datasets",
                "qualitative results and failure cases",
                "clear architecture and training details for reproduction"
              ],
              "accepted_paper_emphasis": [
                "benchmark tables against the current best published numbers",
                "ablation studies over every added component",
                "figures that carry the explanation, not just decorate it"
              ],
              "format_rules": {
                "page_limit": 8,
                "references_in_limit": false,
                "abstract_word_limit": null,
                "anonymous": true,
                "citation_style": "numeric",
                "template": "the official CVPR LaTeX template (cvpr.sty)",
                "required_sections": [],
                "recommended_sections": [],
                "unresolved": []
              },
              "source": "seed",
              "source_url": "https://cvpr.thecvf.com/Conferences/2026/AuthorGuidelines",
              "updated_at": "2026-07-29"
            }
          }
        },
        {
          "module": "FormatComplianceAgent",
          "prompt": {
            "system_prompt": "You are FormatComplianceAgent. You are given deterministic check results for one manuscript, plus any passages retrieved from the venue's Call-for-Papers.\nReturn a JSON object only:\n{\"resolutions\":[{\"rule\":\"<existing rule name>\",\"status\":\"pass\"|\"fail\"|\"warn\"|\"unknown\",\"detail\":\"<what the evidence shows>\",\"suggestion\":\"<concrete, manuscript-specific action>\"}],\n \"note\":\"<one sentence summarising the state of the submission>\"}\nRules:\n- Only emit a resolution for a rule that was given to you; do not invent rules.\n- Change a status away from \"unknown\" only when a retrieved passage states the rule. Otherwise leave it \"unknown\" and say what the author should confirm.\n- Suggestions must be specific to this manuscript (name the section, the count, the offending text). Never restate the rule.\n- The status describes the manuscript the author submitted, so a rule stays \"fail\" even once code has fixed it. When a finding appears under \"Already fixed automatically\", say so in the suggestion and give only the residual action the author still has to take — never tell them to redo work that is already done.",
            "user_prompt": "Venue: CVPR 2026\n\nVenue rules on record: {\"page_limit\":8,\"references_in_limit\":false,\"abstract_word_limit\":null,\"anonymous\":true,\"citation_style\":\"numeric\",\"template\":\"the official CVPR LaTeX template (cvpr.sty)\",\"required_sections\":[],\"recommended_sections\":[],\"unresolved\":[]}\n\nManuscript: 800 words, ~2 page(s), sections: Abstract, Introduction, Related Work, Method, Experiments, Results, Conclusion, Acknowledgements, References\n\nChecks needing a suggestion:\n- anonymity [fail]: Double-blind venue. Found 7 identifying item(s): author / affiliation block, contact email, code or model repository link, institution name in the front matter, de-anonymising self-reference, acknowledgements or funding statement.\n  evidence: author_block: Ann Researcher, Ben Coder, Carla Dev Institute of Technology, Department of Computer Science ann.researcher@example.edu, ben.coder@example.edu\nemail: ann.researcher@example.edu\nemail: ben.coder@example.edu\nrepo_url: https://github.com/ann-researcher/car-routing.\naffiliation: Institute of Technology\nself_reference: Our previous work on expert placement [4] considered the static assign\n- template [warn]: The venue requires the official CVPR LaTeX template (cvpr.sty). ConfFit reads plain text and cannot verify the compiled template.\n\nAlready fixed automatically: Replaced the author/affiliation block with an anonymous placeholder.; Anonymised 1 repository link(s).; Rephrased self-referential citations (\"our prior work\" → \"prior work\").; Removed the Acknowledgements section (restore it for camera-ready).",
            "System_prompt": "You are FormatComplianceAgent. You are given deterministic check results for one manuscript, plus any passages retrieved from the venue's Call-for-Papers.\nReturn a JSON object only:\n{\"resolutions\":[{\"rule\":\"<existing rule name>\",\"status\":\"pass\"|\"fail\"|\"warn\"|\"unknown\",\"detail\":\"<what the evidence shows>\",\"suggestion\":\"<concrete, manuscript-specific action>\"}],\n \"note\":\"<one sentence summarising the state of the submission>\"}\nRules:\n- Only emit a resolution for a rule that was given to you; do not invent rules.\n- Change a status away from \"unknown\" only when a retrieved passage states the rule. Otherwise leave it \"unknown\" and say what the author should confirm.\n- Suggestions must be specific to this manuscript (name the section, the count, the offending text). Never restate the rule.\n- The status describes the manuscript the author submitted, so a rule stays \"fail\" even once code has fixed it. When a finding appears under \"Already fixed automatically\", say so in the suggestion and give only the residual action the author still has to take — never tell them to redo work that is already done.",
            "User_prompt": "Venue: CVPR 2026\n\nVenue rules on record: {\"page_limit\":8,\"references_in_limit\":false,\"abstract_word_limit\":null,\"anonymous\":true,\"citation_style\":\"numeric\",\"template\":\"the official CVPR LaTeX template (cvpr.sty)\",\"required_sections\":[],\"recommended_sections\":[],\"unresolved\":[]}\n\nManuscript: 800 words, ~2 page(s), sections: Abstract, Introduction, Related Work, Method, Experiments, Results, Conclusion, Acknowledgements, References\n\nChecks needing a suggestion:\n- anonymity [fail]: Double-blind venue. Found 7 identifying item(s): author / affiliation block, contact email, code or model repository link, institution name in the front matter, de-anonymising self-reference, acknowledgements or funding statement.\n  evidence: author_block: Ann Researcher, Ben Coder, Carla Dev Institute of Technology, Department of Computer Science ann.researcher@example.edu, ben.coder@example.edu\nemail: ann.researcher@example.edu\nemail: ben.coder@example.edu\nrepo_url: https://github.com/ann-researcher/car-routing.\naffiliation: Institute of Technology\nself_reference: Our previous work on expert placement [4] considered the static assign\n- template [warn]: The venue requires the official CVPR LaTeX template (cvpr.sty). ConfFit reads plain text and cannot verify the compiled template.\n\nAlready fixed automatically: Replaced the author/affiliation block with an anonymous placeholder.; Anonymised 1 repository link(s).; Rephrased self-referential citations (\"our prior work\" → \"prior work\").; Removed the Acknowledgements section (restore it for camera-ready)."
          },
          "response": {
            "resolutions": [
              {
                "rule": "anonymity",
                "status": "fail",
                "detail": "The submitted manuscript still contains identifying content in the front matter and body: contact emails for Ann Researcher and Ben Coder, an institution name (“Institute of Technology”) in the author block, a code repository link, and a de-anonymising self-reference in the text. The report also notes an acknowledgements/funding statement issue, but the Acknowledgements section was already removed automatically.",
                "suggestion": "Manually remove the remaining email addresses from the title page/author block and any footnotes, delete or hide the GitHub link if any version still appears, and replace the self-reference in the Introduction (“Our previous work on expert placement [4]…”) with neutral wording; the author/affiliation block has already been anonymised, so only confirm no other identifying text remains before resubmitting."
              },
              {
                "rule": "template",
                "status": "warn",
                "detail": "The record shows the venue requires the official CVPR LaTeX template (cvpr.sty), but the check only saw plain text and could not verify the compiled template.",
                "suggestion": "Confirm that the submitted source uses cvpr.sty and the CVPR class/style file in the paper preamble, since the current plain-text export cannot demonstrate this."
              }
            ],
            "note": "The submission still fails anonymity due to residual identifiers, while template compliance cannot be verified from the provided plain-text manuscript."
          }
        },
        {
          "module": "UnifiedFixer",
          "prompt": {
            "system_prompt": "Apply the framing and format reports to the manuscript in one pass.",
            "user_prompt": "No framing report and no findings that need judgement.",
            "System_prompt": "Apply the framing and format reports to the manuscript in one pass.",
            "User_prompt": "No framing report and no findings that need judgement."
          },
          "response": {
            "llm_call": false,
            "edits": [],
            "summary": "Only mechanical fixes were required; they were applied in code without a model call.",
            "mechanical_fixes": [
              "Replaced the author/affiliation block with an anonymous placeholder.",
              "Anonymised 1 repository link(s).",
              "Rephrased self-referential citations (\"our prior work\" → \"prior work\").",
              "Removed the Acknowledgements section (restore it for camera-ready)."
            ]
          }
        },
        {
          "module": "Supervisor",
          "prompt": {
            "system_prompt": "You are the Supervisor of ConfFit. Write the opening summary of the agent's answer for the author.\nYou are given a compact digest of what the workers produced. Return a JSON object only:\n{\"summary\":\"<3-5 sentences: the single most important framing move, the count and nature of the format findings, and what was changed in the revised manuscript>\"}\nBe concrete and quantitative. Do not repeat the reports verbatim — they are printed below your summary. Do not invent findings.",
            "user_prompt": "{\"venue\":\"CVPR 2026\",\"task\":\"format\",\"framing\":null,\"format\":{\"counts\":{\"pass\":4,\"fail\":1,\"warn\":1,\"unknown\":0},\"failures\":[\"anonymity\"]},\"revision\":{\"changes\":[\"Replaced the author/affiliation block with an anonymous placeholder.\",\"Anonymised 1 repository link(s).\",\"Rephrased self-referential citations (\\\"our prior work\\\" → \\\"prior work\\\").\",\"Removed the Acknowledgements section (restore it for camera-ready).\"],\"fixer_summary\":\"Applied mechanical fixes only.\"}}",
            "System_prompt": "You are the Supervisor of ConfFit. Write the opening summary of the agent's answer for the author.\nYou are given a compact digest of what the workers produced. Return a JSON object only:\n{\"summary\":\"<3-5 sentences: the single most important framing move, the count and nature of the format findings, and what was changed in the revised manuscript>\"}\nBe concrete and quantitative. Do not repeat the reports verbatim — they are printed below your summary. Do not invent findings.",
            "User_prompt": "{\"venue\":\"CVPR 2026\",\"task\":\"format\",\"framing\":null,\"format\":{\"counts\":{\"pass\":4,\"fail\":1,\"warn\":1,\"unknown\":0},\"failures\":[\"anonymity\"]},\"revision\":{\"changes\":[\"Replaced the author/affiliation block with an anonymous placeholder.\",\"Anonymised 1 repository link(s).\",\"Rephrased self-referential citations (\\\"our prior work\\\" → \\\"prior work\\\").\",\"Removed the Acknowledgements section (restore it for camera-ready).\"],\"fixer_summary\":\"Applied mechanical fixes only.\"}}"
          },
          "response": {
            "summary": "The key framing move is that the revision addresses CVPR 2026 formatting compliance by making the manuscript anonymous and stripping out author-identifying material. Across the format checks, there were 4 passes, 1 failure, and 1 warning, with the only failure being anonymity. In the revised manuscript, the author/affiliation block was replaced with an anonymous placeholder, 1 repository link was anonymized, self-referential citations were rewritten to remove first-person wording, and the Acknowledgements section was removed for the submission version."
          }
        }
      ]
    },
    {
      "scenario": "unknown venue — human-in-the-loop gate",
      "prompt": "Target conference: SIGBOVIK 2027\nTask: both\nPaper: Cache-Aware Expert Routing for Efficient Mixture-of-Experts Inference\n\nAnn Researcher, Ben Coder, Carla Dev\nInstitute of Technology, Department of Computer Science\nann.researcher@example.edu, ben.coder@example.edu\n\nAbstract\nMixture-of-Experts (MoE) models activate only a small subset of their parameters per token, but in practice inference throughput is dominated not by computation but by the movement of expert weights between high-bandwidth memory and the accelerator's on-chip cache. We introduce CAR, a cache-aware routing policy that augments the standard top-k gating function with a residency term reflecting which experts are already resident in cache. CAR requires no retraining of the base model and is implemented as a drop-in replacement for the router at inference time. Across three open MoE checkpoints ranging from 8B to 47B total parameters, CAR reduces expert-weight traffic by 31 to 44 percent and improves end-to-end decoding throughput by 1.6x to 2.1x on a single A100, while changing downstream task accuracy by less than 0.4 points on MMLU, GSM8K and HumanEval. We further show that the residency term can be annealed during a brief 200-step calibration phase to recover alm",
      "full_response": "I don't have guidelines for **SIGBOVIK 2027** in the knowledge base yet.\n\nMay I fetch https://sigbovik.org/ and add it? Reply **yes** to approve, or paste the correct Call-for-Papers link.\n\nOther candidates I found, if that one is wrong:\n- https://www.acm.org/publications/authors/submissions\n- http://sigbovik.org/2024/\n- https://www.besthub.dev/articles/aaai-27-submission-guide-new-rules-and-details-that-could-lead-to-immediate-desk-rejection-6f9ab39403f3\n\nNothing has been written to the knowledge base.",
      "steps": [
        {
          "module": "Supervisor",
          "prompt": {
            "system_prompt": "You are the Supervisor of ConfFit, an agent that adapts a research paper to a target academic conference. Decide how to route one request.\nReturn a JSON object only:\n{\"in_scope\":true|false,\n \"target_conference\":\"<venue name as the user gave it, or null>\",\n \"task\":\"framing\"|\"format\"|\"both\",\n \"is_approval_reply\":true|false,\n \"provided_url\":\"<only a URL the user gave as the venue or its call-for-papers; null if the only URLs are inside the manuscript text>\",\n \"reason\":\"<why the request is out of scope, else null>\"}\nGuidance:\n- in_scope is false only if the request has nothing to do with preparing or adapting an academic paper for a venue.\n- \"framing\" = re-position the paper for the venue. \"format\" = check the submission rules. \"both\" when the user asks for both or does not say.\n- is_approval_reply is true when the message is only approving a previous request to add a venue to the knowledge base (for example \"yes\", \"go ahead\", or a bare CFP link).",
            "user_prompt": "Target conference: SIGBOVIK 2027\nTask: both\nPaper (first 700 chars): Cache-Aware Expert Routing for Efficient Mixture-of-Experts Inference\n\nAnn Researcher, Ben Coder, Carla Dev\nInstitute of Technology, Department of Computer Science\nann.researcher@example.edu, ben.coder@example.edu\n\nAbstract\nMixture-of-Experts (MoE) models activate only a small subset of their parameters per token, but in practice inference throughput is dominated not by computation but by the movement of expert weights between high-bandwidth memory and the accelerator's on-chip cache. We introduce CAR, a cache-aware routing policy that augments the standard top-k gating function with a residency term reflecting which experts are already resident in cache. CAR requires no retraining of the ba",
            "System_prompt": "You are the Supervisor of ConfFit, an agent that adapts a research paper to a target academic conference. Decide how to route one request.\nReturn a JSON object only:\n{\"in_scope\":true|false,\n \"target_conference\":\"<venue name as the user gave it, or null>\",\n \"task\":\"framing\"|\"format\"|\"both\",\n \"is_approval_reply\":true|false,\n \"provided_url\":\"<only a URL the user gave as the venue or its call-for-papers; null if the only URLs are inside the manuscript text>\",\n \"reason\":\"<why the request is out of scope, else null>\"}\nGuidance:\n- in_scope is false only if the request has nothing to do with preparing or adapting an academic paper for a venue.\n- \"framing\" = re-position the paper for the venue. \"format\" = check the submission rules. \"both\" when the user asks for both or does not say.\n- is_approval_reply is true when the message is only approving a previous request to add a venue to the knowledge base (for example \"yes\", \"go ahead\", or a bare CFP link).",
            "User_prompt": "Target conference: SIGBOVIK 2027\nTask: both\nPaper (first 700 chars): Cache-Aware Expert Routing for Efficient Mixture-of-Experts Inference\n\nAnn Researcher, Ben Coder, Carla Dev\nInstitute of Technology, Department of Computer Science\nann.researcher@example.edu, ben.coder@example.edu\n\nAbstract\nMixture-of-Experts (MoE) models activate only a small subset of their parameters per token, but in practice inference throughput is dominated not by computation but by the movement of expert weights between high-bandwidth memory and the accelerator's on-chip cache. We introduce CAR, a cache-aware routing policy that augments the standard top-k gating function with a residency term reflecting which experts are already resident in cache. CAR requires no retraining of the ba"
          },
          "response": {
            "in_scope": true,
            "target_conference": "SIGBOVIK 2027",
            "task": "both",
            "is_approval_reply": false,
            "provided_url": null,
            "reason": null
          }
        },
        {
          "module": "ConferenceProfiler",
          "prompt": {
            "system_prompt": "Check the cache. On a miss, do not ingest — propose a source and return a confirmation request for the user.",
            "user_prompt": "Profile SIGBOVIK 2027 (venue_id=sigbovik-2027).",
            "System_prompt": "Check the cache. On a miss, do not ingest — propose a source and return a confirmation request for the user.",
            "User_prompt": "Profile SIGBOVIK 2027 (venue_id=sigbovik-2027)."
          },
          "response": {
            "llm_call": false,
            "cache_hit": false,
            "wrote_to_knowledge_base": false,
            "proposed_url": "https://sigbovik.org/",
            "web_search": "1. SIGBOVIK - The Association for Computational Heresy — https://sigbovik.org/\n2. Submissions - Association for Computing Machinery — https://www.acm.org/publications/authors/submissions\n3. Sigbovik 2024 — http://sigbovik.org/2024/\n4. AAAI‑27 Submission Guide: New Rules and Details That C… | BestHub — https://www.besthub.dev/articles/aaai-27-submission-guide-new-rules-and-details-that-could-lead-to-immediate-desk-rejection-6f9ab39403f3\n5. Voting - Mentimeter — https://www.menti.com/\n6. CHI Publication Formats - ACM CHI 2027 — https://chi2027.acm.org/chi-publication-formats/",
            "ask_user": "I don't have guidelines for **SIGBOVIK 2027** in the knowledge base yet.\n\nMay I fetch https://sigbovik.org/ and add it? Reply **yes** to approve, or paste the correct Call-for-Papers link.\n\nOther candidates I found, if that one is wrong:\n- https://www.acm.org/publications/authors/submissions\n- http://sigbovik.org/2024/\n- https://www.besthub.dev/articles/aaai-27-submission-guide-new-rules-and-details-that-could-lead-to-immediate-desk-rejection-6f9ab39403f3\n\nNothing has been written to the knowledge base."
          }
        }
      ]
    }
  ]
}
